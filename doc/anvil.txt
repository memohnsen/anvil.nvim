*anvil.txt* A Magit inspired Git porcelain for Neovim
*anvil*

Original Author: TimUntersberger
Current Author: Cameron Kolkey <cameron.kolkey@gmail.com>
Homepage: <https://github.com/memohnsen/anvil.nvim>
License: MIT license

==============================================================================
CONTENTS                                                       *anvil_contents*

    1. Intro                                                      |anvil_intro|
    2. Setup                                                      *anvil_setup*
       • Plugin Setup                                      |anvil_setup_plugin|
       • Mappings                                        |anvil_setup_mappings|
       • GPG Integration                                      |anvil_setup_gpg|
    3. Commands                                                |anvil_commands|
    4. Events                                                    |anvil_events|
    5. Highlights                                            |anvil_highlights|
    6. API                                                          |anvil_api|
       • Popup Builder                                    |anvil_popup_builder|
       • Customizing Popups                               |anvil_custom_popups|
    7. Usage                                                      |anvil_usage|
    8. Popups                                                    *anvil_popups*
       • Bisect                                            |anvil_bisect_popup|
       • Branch                                            |anvil_branch_popup|
       • Branch Config                              |anvil_branch_config_popup|
       • Bundle                                            |anvil_bundle_popup|
       • Cherry Pick                                  |anvil_cherry_pick_popup|
       • Commit                                            |anvil_commit_popup|
       • Diff                                                |anvil_diff_popup|
       • Dispatch                                        |anvil_dispatch_popup|
       • Fetch                                              |anvil_fetch_popup|
       • File Dispatch                              |anvil_file_dispatch_popup|
       • Forge                                              |anvil_forge_popup|
       • Ignore                                            |anvil_ignore_popup|
       • Log                                                  |anvil_log_popup|
       • Mergetool                                      |anvil_mergetool_popup|
       • Merge                                              |anvil_merge_popup|
       • Notes                                              |anvil_notes_popup|
       • Patch                                              |anvil_patch_popup|
       • Pull                                                |anvil_pull_popup|
       • Push                                                |anvil_push_popup|
       • Rebase                                            |anvil_rebase_popup|
       • Remote                                            |anvil_remote_popup|
       • Remote Config                              |anvil_remote_config_popup|
       • Repositories                                      |anvil_repos_popup|
       • Reset                                              |anvil_reset_popup|
       • Revert                                            |anvil_revert_popup|
       • Run                                                  |anvil_run_popup|
       • Shortlog                                      |anvil_shortlog_popup|
       • Sparse Checkout                        |anvil_sparse_checkout_popup|
       • Stash                                              |anvil_stash_popup|
       • Submodule                                      |anvil_submodule_popup|
       • Subtree                                          |anvil_subtree_popup|
       • Tag                                                  |anvil_tag_popup|
       • Worktree                                        |anvil_worktree_popup|
    9. Buffers                                                  *anvil_buffers*
       • Status                                           |anvil_status_buffer|
       • Editor                                           |anvil_editor_buffer|
       • Log                                                 |anvil_log_buffer|
       • Reflog                                           |anvil_reflog_buffer|
       • Refs                                               |anvil_refs_buffer|
       • Commit                                           |anvil_commit_buffer|
       • Rebase Todo                                 |anvil_rebase_todo_buffer|
       • Blame                                             |anvil_blame_buffer|

==============================================================================
1. Intro                                                          *anvil_intro*

Anvil is a git + GitHub client for Neovim, built as a full 1:1 rewrite of the
popular emacs git client 'Magit' and its GitHub integration 'Forge', with a
forge layer inspired by octo.nvim (https://github.com/pwntester/octo.nvim). See
|anvil_forge_popup| for the GitHub integration.

"Magit is a complete text-based user interface to Git. It fills the glaring
gap between the Git command-line interface and various GUIs, letting you
perform trivial as well as elaborate version control tasks with just a couple
of mnemonic key presses. Magit looks like a prettified version of what you get
after running a few Git commands but in Magit every bit of visible information
is also actionable to an extent that goes far beyond what any Git GUI provides
and it takes care of automatically refreshing this output when it becomes
outdated. In the background Magit just runs Git commands and if you wish you
can see what exactly is being run, making it possible for you to learn the git
command-line by using Magit.

Using Magit for a while will make you a more effective version control user.
Magit supports and streamlines the use of Git features that most users and
developers of other Git clients apparently thought could not be reasonably
mapped to a non-command-line interface. Magit is both faster and more
intuitive than either the command line or any GUI and these holds for both
Git beginners and experts alike."

- https://magit.vc/


Though not yet feature complete, our goal is to bring the Magit git experience
to Neovim users.

==============================================================================
2. Plugin Setup                                            *anvil_setup_plugin*

    >lua
    local anvil = require("anvil")

    anvil.setup {
    -- Hides the hints at the top of the status buffer
    disable_hint = false,
    -- Disables changing the buffer highlights based on where the cursor is.
    disable_context_highlighting = false,
    -- Disables signs for sections/items/hunks
    disable_signs = false,
    -- Path to git executable. Defaults to "git". Can be used to specify a custom git binary or wrapper script.
    git_executable = "git",
    -- Offer to force push when branches diverge
    prompt_force_push = true,
    -- Request confirmation when amending already published commits
    prompt_amend_commit = true,
    -- Changes what mode the Commit Editor starts in. `true` will leave nvim in normal mode, `false` will change nvim to
    -- insert mode, and `"auto"` will change nvim to insert mode IF the commit message is empty, otherwise leaving it in
    -- normal mode.
    disable_insert_on_commit = "auto",
    -- When enabled, will watch the `.git/` directory for changes and refresh the status buffer in response to filesystem
    -- events.
    filewatcher = {
        interval = 1000,
        enabled = true,
    },
    -- "ascii"   is the graph the git CLI generates
    -- "unicode" is the graph like https://github.com/rbong/vim-flog
    -- "kitty"   is the graph like https://github.com/isakbm/gitgraph.nvim - use https://github.com/rbong/flog-symbols if you don't use Kitty
    graph_style = "ascii",
    -- Show relative date by default. When set, use `strftime` to display dates
    commit_date_format = nil,
    log_date_format = nil,
    -- When set, used to format the diff. Requires `baleia` to colorize text with ANSI escape sequences. An example for
    -- `Delta` is `{ 'delta', '--width', '117' }`. For `Delta`, hyperlinks must be disabled when called by `anvil`, for text to be colorized properly.
    log_pager = nil,
    -- Used to generate URL's for branch popup action "pull request" or opening a commit.
    git_services = {
        ["github.com"] = {
            pull_request = "https://github.com/${owner}/${repository}/compare/${branch_name}?expand=1",
            commit = "https://github.com/${owner}/${repository}/commit/${oid}",
            tree = "https://${host}/${owner}/${repository}/tree/${branch_name}",
        },
        ["bitbucket.org"] = {
            pull_request = "https://bitbucket.org/${owner}/${repository}/pull-requests/new?source=${branch_name}&t=1",
            commit = "https://bitbucket.org/${owner}/${repository}/commits/${oid}",
            tree = "https://bitbucket.org/${owner}/${repository}/branch/${branch_name}",
        },
        ["gitlab.com"] = {
            pull_request = "https://gitlab.com/${owner}/${repository}/merge_requests/new?merge_request[source_branch]=${branch_name}",
            commit = "https://gitlab.com/${owner}/${repository}/-/commit/${oid}",
            tree = "https://gitlab.com/${owner}/${repository}/-/tree/${branch_name}?ref_type=heads",
        },
        ["azure.com"] = {
            pull_request = "https://dev.azure.com/${owner}/_git/${repository}/pullrequestcreate?sourceRef=${branch_name}&targetRef=${target}",
            commit = "",
            tree = "",
        },
        ["codeberg.org"] = {
          pull_request = "https://${host}/${owner}/${repository}/compare/${branch_name}",
          commit = "https://${host}/${owner}/${repository}/commit/${oid}",
          tree = "https://${host}/${owner}/${repository}/src/branch/${branch_name}",
        },
    },
    -- Allows a different telescope sorter. Defaults to 'fuzzy_with_index_bias'. The example below will use the native fzf
    -- sorter instead. By default, this function returns `nil`.
    telescope_sorter = function()
        return require("telescope").extensions.fzf.native_fzf_sorter()
    end,
    -- Persist the values of switches/options within and across sessions
    remember_settings = true,
    -- Scope persisted settings on a per-project basis
    use_per_project_settings = true,
    -- Table of settings to never persist. Uses format "Filetype--cli-value"
    ignored_settings = {},
    -- Configure highlight group features
    highlight = {
        italic = true,
        bold = true,
        underline = true
    },
    -- Set to false if you want to be responsible for creating _ALL_ keymappings
    use_default_keymaps = true,
    -- Anvil refreshes its internal state after specific events, which can be expensive depending on the repository size.
    -- Disabling `auto_refresh` will make it so you have to manually refresh the status after you open it.
    auto_refresh = true,
    -- Value used for `--sort` option for `git branch` command
    -- By default, branches will be sorted by commit date descending
    -- Flag description: https://git-scm.com/docs/git-branch#Documentation/git-branch.txt---sortltkeygt
    -- Sorting keys: https://git-scm.com/docs/git-for-each-ref#_options
    sort_branches = "-committerdate",
    -- Value passed to the `--<commit_order>-order` flag of the `git log` command
    -- Determines how commits are traversed and displayed in the log / graph:
    --   "topo"         topological order (parents always before children, good for graphs, slower on large repos)
    --   "date"         chronological order by commit date
    --   "author-date"  chronological order by author date
    --   ""             disable explicit ordering (fastest, recommended for very large repos)
    commit_order = "topo"
    -- Default for new branch name prompts
    initial_branch_name = "",
    -- Default for rename branch prompt. If not set, the current branch name is used
    initial_branch_rename = nil,
    -- Change the default way of opening anvil
    kind = "tab",
    -- Floating window style
    floating = {
      relative = "editor",
      width = 0.8,
      height = 0.7,
      style = "minimal",
      border = "rounded",
    },
    -- Disable line numbers
    disable_line_numbers = true,
    -- Disable relative line numbers
    disable_relative_line_numbers = true,
    -- The time after which an output console is shown for slow running commands
    console_timeout = 2000,
    -- Automatically show console if a command takes more than console_timeout milliseconds
    auto_show_console = true,
    -- Automatically close the console if the process exits with a 0 (success) status
    auto_close_console = true,
    notification_icon = "󰊢",
    status = {
        show_head_commit_hash = true,
        recent_commit_count = 10,
        HEAD_padding = 10,
        HEAD_folded = false,
        mode_padding = 3,
        mode_text = {
        M = "modified",
        N = "new file",
        A = "added",
        D = "deleted",
        C = "copied",
        U = "updated",
        R = "renamed",
        T = "changed",
        DD = "unmerged",
        AU = "unmerged",
        UD = "unmerged",
        UA = "unmerged",
        DU = "unmerged",
        AA = "unmerged",
        UU = "unmerged",
        ["?"] = "",
        },
    },
    commit_editor = {
        kind = "tab",
        show_staged_diff = true,
        -- Accepted values:
        -- "split" to show the staged diff below the commit editor
        -- "vsplit" to show it to the right
        -- "split_above" Like :top split
        -- "vsplit_left" like :vsplit, but open to the left
        -- "auto" "vsplit" if window would have 80 cols, otherwise "split"
        staged_diff_split_kind = "split",
        spell_check = true,
    },
    commit_select_view = {
        kind = "tab",
    },
    commit_view = {
        kind = "vsplit",
        verify_commit = vim.fn.executable("gpg") == 1, -- Can be set to true or false, otherwise we try to find the binary
    },
    log_view = {
        kind = "tab",
    },
    rebase_editor = {
        kind = "auto",
    },
    reflog_view = {
        kind = "tab",
    },
    merge_editor = {
        kind = "auto",
    },
    preview_buffer = {
        kind = "floating_console",
    },
    popup = {
        kind = "split",
        show_title = false,
    },
    stash = {
        kind = "tab",
    },
    refs_view = {
        kind = "tab",
    },
    signs = {
        -- { CLOSED, OPENED }
        hunk = { "", "" },
        item = { ">", "v" },
        section = { ">", "v" },
    },
    -- Each Integration is auto-detected through plugin presence, however, it can be disabled by setting to `false`
    integrations = {
        -- If enabled, use telescope for menu selection rather than vim.ui.select.
        -- Allows multi-select and some things that vim.ui.select doesn't.
        telescope = nil,
        -- Anvil only provides inline diffs. If you want a more traditional way to look at diffs, you can use `diffview`.
        -- The diffview integration enables the diff popup.
        --
        -- Requires you to have `sindrets/diffview.nvim` installed.
        diffview = nil,

        -- Alternative diff viewer integration.
        -- Requires you to have `esmuellert/codediff.nvim` installed.
        codediff = nil,

        -- If enabled, uses fzf-lua for menu selection. If the telescope integration
        -- is also selected then telescope is used instead
        -- Requires you to have `ibhagwan/fzf-lua` installed.
        fzf_lua = nil,

        -- If enabled, uses mini.pick for menu selection. If the telescope integration
        -- is also selected then telescope is used instead
        -- Requires you to have `echasnovski/mini.pick` installed.
        mini_pick = nil,

        -- If enabled, uses snacks.picker for menu selection. If the telescope integration
        -- is also selected then telescope is used instead
        -- Requires you to have `folke/snacks.nvim` installed.
        snacks = nil,
    },
    -- Which diff viewer to use. nil = auto-detect (tries diffview first, then codediff).
    -- Can be "diffview" or "codediff".
    diff_viewer = nil,
    sections = {
        -- Reverting/Cherry Picking
        sequencer = {
        folded = false,
        hidden = false,
        },
        untracked = {
        folded = false,
        hidden = false,
        },
        unstaged = {
        folded = false,
        hidden = false,
        },
        staged = {
        folded = false,
        hidden = false,
        },
        stashes = {
        folded = true,
        hidden = false,
        },
        unpulled_upstream = {
        folded = true,
        hidden = false,
        },
        unmerged_upstream = {
        folded = false,
        hidden = false,
        },
        unpulled_pushRemote = {
        folded = true,
        hidden = false,
        },
        unmerged_pushRemote = {
        folded = false,
        hidden = false,
        },
        recent = {
        folded = true,
        hidden = false,
        },
        rebase = {
        folded = true,
        hidden = false,
        },
        -- Forge: open pull requests (synced via the forge popup, "N f")
        pullreqs = {
        folded = true,
        hidden = false,
        },
        -- Forge: open issues
        issues = {
        folded = true,
        hidden = false,
        },
    },
    }
    >

==============================================================================
Commit Signing / GPG Integration                              *anvil_setup_gpg*

If you sign commits using gnugpg, there are a few steps that need to be taken
to properly integrate the password authentication with Anvil:
  >gpg
  # ~/.gnupg/gpg-agent.conf
  pinentry-program /opt/homebrew/bin/pinentry-tty
  allow-loopback-pinentry
<
  >gpg
  # ~/.gnupg/gpg.conf
  pinentry-mode loopback
<

  Note: If you are not using Homebrew you may need to change the path for
        `pinentry-program

  Note: The location of these config files may not be in "~/.gnupg/" depending
        on your system configuration. To find where they should be placed run
        "gpgconf --list-dirs" and place them in the path which follows the
        line starting "homedir:". For example this could be
        "$XDG_DATA_HOME/gnupg/"

==============================================================================
Mappings                                                 *anvil_setup_mappings*

The following mappings can all be customized via the setup function.
>lua
      commit_editor = {
        ["q"]          = "Close",
        ["<m-p>"]      = "PrevMessage",
        ["<m-n>"]      = "NextMessage",
        ["<m-r>"]      = "ResetMessage",
        ["<c-c><c-c>"] = "Submit",
        ["<c-c><c-k>"] = "Abort",
      }
      commit_editor_I = {
        ["<c-c><c-c>"] = "Submit",
        ["<c-c><c-k>"] = "Abort",
      }

      commit_view = {
        ["a"]          = "OpenFileInWorktree",
      }

      rebase_editor = {
        ["p"]          = "Pick",
        ["r"]          = "Reword",
        ["e"]          = "Edit",
        ["s"]          = "Squash",
        ["f"]          = "Fixup",
        ["x"]          = "Execute",
        ["d"]          = "Drop",
        ["b"]          = "Break",
        ["q"]          = "Close",
        ["<cr>"]       = "OpenCommit",
        ["gk"]         = "MoveUp",
        ["gj"]         = "MoveDown",
        ["<c-c><c-c>"] = "Submit",
        ["<c-c><c-k>"] = "Abort",
        ["[c"]         = "OpenOrScrollUp",
        ["]c"]         = "OpenOrScrollDown",
      }
      rebase_editor_I = {
        ["<c-c><c-c>"] = "Submit",
        ["<c-c><c-k>"] = "Abort",
      }

      finder = {
        ["<cr>"]               = "Select",
        ["<c-c>"]              = "Close",
        ["<esc>"]              = "Close",
        ["<c-n>"]              = "Next",
        ["<c-p>"]              = "Previous",
        ["<down>"]             = "Next",
        ["<up>"]               = "Previous",
        ["<tab>"]              = "InsertCompletion",
        ["<c-y>"]              = "CopySelection",
        ["<space>"]            = "MultiselectToggleNext",
        ["<s-space>"]          = "MultiselectTogglePrevious",
        ["<c-j>"]              = "NOP",
        ["<ScrollWheelDown>"]  = "ScrollWheelDown",
        ["<ScrollWheelUp>"]    = "ScrollWheelUp",
        ["<ScrollWheelLeft>"]  = "NOP",
        ["<ScrollWheelRight>"] = "NOP",
        ["<LeftMouse>"]        = "MouseClick",
        ["<2-LeftMouse>"]      = "NOP",
      }

      popup = {
        ["?"] = "HelpPopup",
        ["A"] = "CherryPickPopup",
        ["d"] = "DiffPopup",
        ["M"] = "RemotePopup",
        ["P"] = "PushPopup",
        ["X"] = "ResetPopup",
        ["Z"] = "WorktreePopup",
        ["z"] = "StashPopup",
        ["i"] = "IgnorePopup",
        ["t"] = "TagPopup",
        ["b"] = "BranchPopup",
        ["B"] = "BisectPopup",
        ["c"] = "CommitPopup",
        ["f"] = "FetchPopup",
        ["l"] = "LogPopup",
        ["m"] = "MergePopup",
        ["p"] = "PullPopup",
        ["r"] = "RebasePopup",
        ["v"] = "RevertPopup",
        ["!"] = "RunPopup",
        ["N"] = "ForgePopup",
        ["W"] = "PatchPopup",
        ["T"] = "NotesPopup",
        ["O"] = "SubmodulePopup",
        ["C"] = "ClonePopup",
        ["h"] = "DispatchPopup",
        ["e"] = "MergetoolPopup",
      }

      status = {
        ["j"]      = "MoveDown",
        ["k"]      = "MoveUp",
        ["o"]      = "OpenTree",
        ["q"]      = "Close",
        ["I"]      = "InitRepo",
        ["1"]      = "Depth1",
        ["2"]      = "Depth2",
        ["3"]      = "Depth3",
        ["4"]      = "Depth4",
        ["Q"]      = "Command",
        ["<tab>"]  = "Toggle",
        ["x"]      = "Discard",
        ["-"]      = "Reverse",
        ["s"]      = "Stage",
        ["S"]      = "StageUnstaged",
        ["<c-s>"]  = "StageAll",
        ["u"]      = "Unstage",
        ["K"]      = "Untrack",
        ["U"]      = "UnstageStaged",
        ["y"]      = "ShowRefs",
        ["$"]      = "CommandHistory",
        ["Y"]      = "YankSelected",
        ["gp"]      = "GoToParentRepo",
        ["<c-r>"]  = "RefreshBuffer",
        ["<cr>"]   = "GoToFile",
        ["<s-cr>"] = "PeekFile",
        ["<c-v>"]  = "VSplitOpen",
        ["<c-x>"]  = "SplitOpen",
        ["<c-t>"]  = "TabOpen",
        ["{"]      = "GoToPreviousHunkHeader",
        ["}"]      = "GoToNextHunkHeader",
        ["[c"]     = "OpenOrScrollUp",
        ["]c"]     = "OpenOrScrollDown",
        ["<c-k>"]  = "PeekUp",
        ["<c-j>"]  = "PeekDown",
      }
<
==============================================================================
3. Commands                                                    *anvil_commands*

                                                                       *:Anvil*
:Anvil                 In a Git repository, opens a new AnvilStatus tab.

                                                             *:AnvilResetState*
:AnvilResetState       Performs a full reset of saved flags for all popups.

                                                                    *:AnvilLog*
:AnvilLog              Opens log buffer for any changes to the current file,
                        or a path the user has specified. Optionally, a range
                        can be specified to further filter the log entries.

                                                                 *:AnvilCommit*
:AnvilCommit           Opens the commit view for the specified SHA, or `HEAD`
                        if left blank.

                                                                 *:Anvil-forge*
:Anvil forge           Opens the forge popup. See |anvil_forge_popup|.

                                                                   *:Anvil-run*
:Anvil run             Opens the run popup for arbitrary git/shell commands.
                        See |anvil_run_popup|.

                                                                 *:Anvil-blame*
:Anvil blame           Opens the blame view for the current file.
                        See |anvil_blame_buffer|.

                                                                 *:Anvil-patch*
:Anvil patch           Opens the patch/am popup. See |anvil_patch_popup|.

                                                                  *:Anvil-notes*
:Anvil notes           Opens the git-notes popup. See |anvil_notes_popup|.

                                                              *:Anvil-submodule*
:Anvil submodule       Opens the submodule popup. See |anvil_submodule_popup|.

                                                                  *:Anvil-clone*
:Anvil clone           Opens the clone popup.

                                                          *:Anvil-file_dispatch*
:Anvil file_dispatch   Opens the file-oriented dispatch popup.
                        See |anvil_file_dispatch_popup|.

                                                        *:Anvil-sparse_checkout*
:Anvil sparse_checkout Opens the sparse-checkout popup.
                        See |anvil_sparse_checkout_popup|.

                                                                *:Anvil-subtree*
:Anvil subtree         Opens the subtree popup. See |anvil_subtree_popup|.

                                                                 *:Anvil-bundle*
:Anvil bundle          Opens the bundle popup. See |anvil_bundle_popup|.

                                                               *:Anvil-shortlog*
:Anvil shortlog        Opens the shortlog popup. See |anvil_shortlog_popup|.

                                                                  *:Anvil-repos*
:Anvil repos           Opens the repository list popup. See |anvil_repos_popup|.

                                                               *:Anvil-dispatch*
:Anvil dispatch        Opens the global dispatch popup.
                        See |anvil_dispatch_popup|.

                                                              *:Anvil-mergetool*
:Anvil mergetool       Opens the mergetool popup. See |anvil_mergetool_popup|.

==============================================================================
4. Events                                                        *anvil_events*

The following events are emitted by Anvil:

• `AnvilStatusRefreshed`
  When: Status has been reloaded
  Data: `{}`

• `AnvilCommitComplete`
  When: Commit has been created
  Data: `{}`

• `AnvilPushComplete`
  When: Push has finished
  Data: `{}`

• `AnvilPullComplete`
  When: Push has finished
  Data: `{}`

• `AnvilFetchComplete`
  When: Fetch has finished
  Data: `{}`

• `AnvilBranchCreate`
  When: Branch was created, starting from `<base>`
  Data: >
    {
      branch_name: string,
      base: string?
    }
<
• `AnvilBranchDelete`
  When: Branch was deleted
  Data: >
    {
      branch_name: string,
    }
<
• `AnvilBranchCheckout`
  When: Branch was checked out
  Data: >
    {
      branch_name: string,
    }
<
• `AnvilBranchReset`
  When: Branch was reset to commit/branch
  Data: >
    {
      branch_name: string,
      resetting_to: string
    }
<
• `AnvilBranchRename`
  When: Branch was renamed
  Data: >
    {
      branch_name: string,
      new_name: string
    }
<
• `AnvilRebase`
  When: A rebase has finished
  Data: >
    {
      commit: string,
      status: "ok" | "conflict"
    }
<
• `AnvilReset`
  When: A reset has been performed
  Data: >
    {
      commit: string,
      mode: "soft" | "mixed" | "hard" | "keep" | "index"
    }
<
• `AnvilTagCreate`
  When: A tag is placed on a commit
  Data: >
    {
      ref: string,
      name: string
    }
<
• `AnvilTagCreate`
  When: A tag is placed on a commit
  Data: >
    {
      ref: string,
      name: string
    }
<
• `AnvilTagDelete`
  When: A tag is removed
  Data: >
    {
      name: string
    }
<
• `AnvilCherryPick`
  When: One or more commits were cherry picked
  Data: >
    {
      commits: string[]
    }
<
• `AnvilMerge`
  When: A merge has finished
  Data: >
    {
      branch: string,
      args: string[],
      status: "ok" | "conflict"
    }
<
• `AnvilStash`
  When: A stash was performed
  Data: >
    {
      success: boolean
    }
<
• `AnvilWorktreeCreate`
  When: A worktree was created
  Data: >
    {
      old_cwd: string,
      new_cwd: string,
      copy_if_present: function(filename: string, callback: function|nil)
    }
<
• `AnvilDiffLoaded`
  When: A hunk is lazy loaded in the status buffer
  Data: >
    {
        item = {
            absolute_path: string,
            relative_path: string,
            row_start: int (First row in buffer the hunk occupies),
            row_end: int (last row in buffer the hunk occupies),
            mode: string (file mode)
        },
        diff = {
            kind: str,
            lines: string[],
            hunks: Hunk[]
        }
    }

    Hunk:
        lines: str[]
        original_lines: str[] (lines from diff not prefixed with +/-)
        modified_lines: str[] (lines from diff, but with +/- prefix removed)
        row_start: int (first line of hunk in the buffer)
        row_end: int (last line of hunk in the buffer)
        header: string (diff header)
<
• `AnvilForgePulled`
  When: Forge topics (pull requests + issues) were synced from GitHub
  Data: `{}`

• `AnvilForgePullRequestCheckout`
  When: A pull request branch was checked out via the forge popup
  Data: >
    {
      number: number
    }
<
• `AnvilUserCommandComplete`
  When: A command started from the run popup finished
  Data: >
    {
      cmd: string,
      cwd: string
    }
<
==============================================================================
5. Highlights                                                *anvil_highlights*

To provide a custom color palette directly to the plugin, you can use the
`config.highlight` table with the following signature: >lua

    ---@class HighlightOptions
    ---@field italic?     boolean
    ---@field bold?       boolean
    ---@field underline?  boolean
    ---@field bg0?        string  Darkest background color
    ---@field bg1?        string  Second darkest background color
    ---@field bg2?        string  Second lightest background color
    ---@field bg3?        string  Lightest background color
    ---@field grey?       string  middle grey shade for foreground
    ---@field white?      string  Foreground white (main text)
    ---@field red?        string  Foreground red
    ---@field bg_red?     string  Background red
    ---@field line_red?   string  Cursor line highlight for red regions
    ---@field orange?     string  Foreground orange
    ---@field bg_orange?  string  background orange
    ---@field yellow?     string  Foreground yellow
    ---@field bg_yellow?  string  background yellow
    ---@field green?      string  Foreground green
    ---@field bg_green?   string  Background green
    ---@field line_green? string  Cursor line highlight for green regions
    ---@field cyan?       string  Foreground cyan
    ---@field bg_cyan?    string  Background cyan
    ---@field blue?       string  Foreground blue
    ---@field bg_blue?    string  Background blue
    ---@field purple?     string  Foreground purple
    ---@field bg_purple?  string  Background purple
    ---@field md_purple?  string  Background medium purple
<

The following highlight groups will all be derived from this palette.

The following highlight groups are defined by this plugin. If you set any of
these yourself before the plugin loads, that will be respected. If they do not
exist, they will be created with sensible defaults based on your colorscheme.

STATUS BUFFER
AnvilNormal                Normal text
AnvilFloat                 Normal text when using a floating window
AnvilFloatBorder           Border wen using a floating window
AnvilBranch                Local branches
AnvilBranchHead            Accent highlight for current HEAD in LogBuffer
AnvilRemote                Remote branches
AnvilObjectId              Object's SHA hash
AnvilStash                 Stash name
AnvilFold                  Folded text highlight
AnvilFoldColumn            Column where folds are displayed
AnvilSignColumn            Column where signs are displayed
AnvilRebaseDone            Current position within rebase
AnvilTagName               Closest Tag name
AnvilTagDistance           Number of commits between the tag and HEAD
AnvilStatusHEAD            The left text in the HEAD section

STATUS BUFFER SECTION HEADERS
AnvilSectionHeader
AnvilUnpushedTo            Linked to AnvilSectionHeader
AnvilUnmergedInto          ^
AnvilUnpulledFrom          ^
AnvilUntrackedfiles        ^
AnvilUnstagedchanges       ^
AnvilUnmergedchanges       ^
AnvilUnpushedchanges       ^
AnvilUnpulledchanges       ^
AnvilRecentcommits         ^
AnvilStagedchanges         ^
AnvilStashes               ^
AnvilRebasing              ^
AnvilReverting             ^
AnvilPicking               ^
AnvilMerging               ^
AnvilBisecting             ^
AnvilSectionHeaderCount    The number, for sections with a number.

STATUS BUFFER FILE
Applied to the label on the left of filenames. These highlight groups are not
used directly, but linked to by other groups:

AnvilChangeModified
AnvilChangeAdded
AnvilChangeDeleted
AnvilChangeRenamed
AnvilChangeUpdated
AnvilChangeCopied
AnvilChangeNewFile
AnvilChangeUnmerged

Styling one of the above groups will apply to all sections, and is generally
whats recommended. However, if you want to control the style on a per-section
basis, the _actual_ highlight groups on the labels follow this pattern:
    `AnvilChange<mode><section>`

Where `<mode>` is one of: (corresponding to the git mode)
  M
  A
  N
  D
  C
  U
  R
  DD
  UU
  AA
  DU
  UD
  AU
  UA

And `<section>` is one of:
  untracked
  unstaged
  staged

So, a modified and staged change would use the `AnvilChangeMstaged` highlight
group, which is linked to `AnvilChangeModified` by default.

SIGNS FOR LINE HIGHLIGHTING
Used to highlight different sections of the status buffer or commit buffer.

AnvilHunkHeader
AnvilDiffContext
AnvilDiffAdd
AnvilDiffDelete
AnvilDiffHeader
AnvilActiveItem            Highlight of current commit-ish open

SIGNS FOR LINE HIGHLIGHTING CURRENT CONTEXT
These are essentially an accented version of the above highlight groups. Only
applies to the current context the cursor is within.

The "Cursor" suffix applies only to the Cursor line

AnvilHunkHeaderHighlight
AnvilDiffContextHighlight
AnvilDiffAddHighlight
AnvilDiffDeleteHighlight
AnvilDiffHeaderHighlight
AnvilHunkHeaderCursor
AnvilDiffContextCursor
AnvilDiffAddCursor
AnvilDiffDeleteCursor
AnvilDiffHeaderCursor

INLINE DIFF
Groups for showing word-level diff in a hunk

AnvilDiffAddInline
AnvilDiffDeleteInline

COMMIT BUFFER
AnvilFilePath              Applied to filepath
AnvilCommitViewHeader      Applied to header of Commit View

LOG VIEW BUFFER
AnvilGraphAuthor           Applied to the commit's author in graph view
AnvilGraphBlack            Used when --colors is enabled for graph
AnvilGraphBoldBlack
AnvilGraphRed
AnvilGraphBoldRed
AnvilGraphGreen
AnvilGraphBoldGreen
AnvilGraphYellow
AnvilGraphBoldYellow
AnvilGraphBlue
AnvilGraphBoldBlue
AnvilGraphPurple
AnvilGraphBoldPurple
AnvilGraphCyan
AnvilGraphBoldCyan
AnvilGraphWhite
AnvilGraphBoldWhite
AnvilGraphGray
AnvilGraphBoldGray
AnvilGraphOrange

AnvilSignatureGood        For highlighting the commit hash when
AnvilSignatureBad         --show-signature is passed
AnvilSignatureMissing
AnvilSignatureNone
AnvilSignatureGoodUnknown
AnvilSignatureGoodExpired
AnvilSignatureGoodExpiredKey
AnvilSignatureGoodRevokedKey

POPUPS
AnvilPopupSectionTitle    Applied to all section headers
AnvilPopupBranchName      Applied to the current branch name for emphasis
AnvilPopupBold            Applied on "@{upstream}" and "pushRemote" for
                           emphasis (but less emphasis than BranchName)

AnvilPopupSwitchKey       Applied to the key that will toggle switch
AnvilPopupSwitchEnabled   Applied to the flag if enabled
AnvilPopupSwitchDisabled  Applied to the flag if disabled

AnvilPopupOptionKey       Applied to the key that will trigger option
AnvilPopupOptionEnabled   Applied if option is set
AnvilPopupOptionDisabled  Applied if option has no value

AnvilPopupConfigKey       Applied to the key that triggers config
AnvilPopupConfigEnabled   Applied to enabled config value
AnvilPopupConfigDisabled  Applied to config without value

AnvilPopupActionKey       Applied to key that triggers function
AnvilPopupActionDisabled  Applied to key when function is unimplemented

COMMAND HISTORY BUFFER
AnvilCommandText          Git command that was run
AnvilCommandTime          Execution time
AnvilCommandCodeNormal    Applied to a successful command's exit status (0)
AnvilCommandCodeError     When command exits with non-zero status

COMMIT SELECT BUFFER
AnvilFloatHeader          Foreground/Background for header text at top of win
AnvilFloatHeaderHighlight Emphasized text in header
==============================================================================
6. Lua API                                               *anvil_api* *anvil-lua*

anvil.open({*opts})                                             *anvil.open()*
    Open Anvil. Alternative to `:Anvil` >lua
        local anvil = require('anvil')

        -- open using defaults
        anvil.open()

        -- open commit popup
        anvil.open({ "commit" })

        -- open with split kind
        anvil.open({ kind = "split" })

        -- open home directory
        anvil.open({ cwd = "~" })
<
    Parameters:
      • {opts}  (table|nil)
                • kind (string) optional: How to open Anvil.
                    - "tab" (default) like :tab split
                    - "replace"       like :enew
                    - "split"         like :below split
                    - "split_above"   like :top split
                    - "vsplit"        like :vsplit
                    - "vsplit_left"   like :vsplit, but open to the left
                    - "floating" not-yet-implemented

                • cwd (string) optional: Path to git repository.
                  default: |current-directory|.
                • no_expand (boolean) optional: Set to true to
                  prevent the calling of `expand()` on the path.

                • [1] (string) optional: Name of the Anvil
                  popup to open. See |anvil-popups|.

anvil.action({popup}, {action}, {args})                       *anvil.action()*
    Call an action without going through the popup interface.

    Note: Some actions might not play nicely when being invoked this way.
          You've been warned. >lua

        local anvil = require('anvil')

        -- Calls the "commit" action from the "commit" popup, with arguments
        vim.keymap.set(
          'n',
          '<leader>gcc',
          anvil.action('commit', 'commit', { '--verbose', '--all' })
        )
<
    Parameters:
      • {popup}  (string)
        Name of popup, as found in `lua/anvil/popups/*`

      • {action} (string)
        Name of action for popup, found in `lua/anvil/popups/*/actions.lua`

      • {args}   (table|nil)
        CLI arguments to pass to git command


==============================================================================
7. Usage                                                          *anvil_usage*

Open Anvil with the |:Anvil| command or `require("anvil").open()`. The status
buffer (see |anvil_status_buffer|) is the hub: it shows the current branch,
staged/unstaged/untracked changes, stashes, unpulled/unmerged commits, recent
commits, and — when the Forge subsystem is active — pull request and issue
sections.

From the status buffer (and most other Anvil buffers) single keys open
transient "popup" menus for git operations, in the style of Magit. Press `?`
to open the help popup, which lists the available popups and their keys. Common
entry points:

  • `c` commit, `p` pull, `P` push, `f` fetch, `b` branch, `m` merge,
    `r` rebase, `l` log, `z` stash, `X` reset, `Z` worktree.
  • `s`/`u` stage/unstage the change under the cursor or selection, `x` discard,
    `<cr>` open the file, `<tab>` toggle a section/hunk fold.
  • `N` opens the Forge popup (GitHub pull requests and issues; see
    |anvil_forge_popup|), and `!` opens the run popup for arbitrary git/shell
    commands.

Popups act on whatever is under the cursor or visually selected — for example,
selecting a range of commits in the log buffer and choosing `pick` from the
cherry-pick popup cherry-picks that range. Popup arguments and the transient
level can be tuned per-popup with the `<C-x>` bindings documented under
|anvil_popup_builder|.

==============================================================================
Bisect Popup                                               *anvil_bisect_popup*

Opened with `B` from the status/log buffers or |:Anvil| `kind=bisect`. Drives
`git bisect` to find the commit that introduced a regression.

Before a bisect is in progress:

Arguments:
  • `-r` `--no-checkout`     Don't checkout commits during the bisect.
  • `-p` `--first-parent`    Follow only the first parent of merge commits.

Actions:
  • `B` Start      Begin a bisect (prompts for known-bad and known-good revs).
  • `S` Scripted   Start a bisect and drive it with a script/command.

While a bisect is in progress the popup instead offers:
  • `b` Bad        Mark the current revision as bad.
  • `g` Good       Mark the current revision as good.
  • `s` Skip       Skip the current revision.
  • `S` Run script Automatically bisect using a script/command.
  • `r` Reset      End the bisect and return to the original HEAD.

==============================================================================
Cherry-Pick Popup                                     *anvil_cherry_pick_popup*

Arguments:                                       *anvil_cherry_pick_popup_args*
  • --ff
    If the current HEAD is the same as the parent of the cherry-pick’ed
    commit, then a fast forward to this commit will be performed.

Actions:                                      *anvil_cherry_pick_popup_actions*
  Actions for the branch popup are split into two categories: "Apply here",
  and "Apply elsewhere". The former will update the current HEAD, whereas the
  latter will prompt for a target.

  When a Cherry-Pick isn't in progress, the following actions are available:

  • Pick                                               *anvil_cherry_pick_pick*
    This command copies COMMITS from another branch onto the current branch.
    If a commit is under the cursor, or multiple commits are selected, then
    those are used instead of prompting. Otherwise the user is prompted to
    select one or more commits.

  • Apply                                             *anvil_cherry_pick_apply*
    This command applies the changes in COMMITS from another branch onto the
    current branch, without committing. If a commit is under the cursor, or
    multiple commits are selected, then those are used instead of prompting.
    Otherwise the user is prompted to select one or more commits.

  • Harvest                                         *anvil_cherry_pick_harvest*
    This command moves the selected COMMITS that must be located on another
    BRANCH onto the current branch instead, removing them from the former.
    When this command succeeds, then the same branch is current as before.

    Applying the commits on the current branch or removing them from the other
    branch can lead to conflicts. When that happens, then this command stops
    and you have to resolve the conflicts and then finish the process manually.

  • Squash                                           *anvil_cherry_pick_squash*
    See: |anvil_merge_squash|

  • Donate                                           *anvil_cherry_pick_donate*
    This command moves the selected COMMITS from the current branch onto
    another existing BRANCH, removing them from the former. When this command
    succeeds, then the same branch is current as before.

    HEAD is allowed to be detached initially.

    Applying the commits on the other branch or removing them from the current
    branch can lead to conflicts. When that happens, then this command stops
    and you have to resolve the conflicts and then finish the process manually.

  • Spinout                                         *anvil_cherry_pick_spinout*
    This command moves the selected COMMITS from the current branch onto a new
    branch BRANCH, removing them from the former. When this command succeeds,
    then the same branch is current as before.

    Applying the commits on the other branch or removing them from the current
    branch can lead to conflicts. When that happens, then this command stops
    and you have to resolve the conflicts and then finish the process manually.

  • Spinoff                                         *anvil_cherry_pick_spinoff*
    This command moves the selected COMMITS from the current branch onto a new
    branch BRANCH, removing them from the former. When this command succeeds,
    then the new branch is checked out.

    Applying the commits on the other branch or removing them from the current
    branch can lead to conflicts. When that happens, then this command stops
    and you have to resolve the conflicts and then finish the process manually.


  When a Cherry-Pick is in progress, the following actions are available:

  • Continue                                       *anvil_cherry_pick_continue*
    Resume current cherry-pick sequence.

  • Skip                                               *anvil_cherry_pick_skip*
    Skip the current commit during a cherry-pick sequence.

  • Abort                                             *anvil_cherry_pick_abort*
    Abort the current cherry-pick sequence. This discards all changes made.

==============================================================================
Branch Popup                                               *anvil_branch_popup*

Variables:                                       *anvil_branch_popup_variables*
  • branch.<name>.description
    Longform description of a branch.

  • branch.<name>.merge, branch.<name>.remote
    Together branch.<name>.merge and branch.<name>.remote define the upstream
    branch for the given branch. It tells git fetch/git pull/git rebase which
    branch to merge and can also affect git push. When in branch <name>, it
    tells git fetch the default refspec to be marked for merging in
    FETCH_HEAD.

    The value is handled like the remote part of a refspec, and must match a
    ref which is fetched from the remote given by "branch.<name>.remote".

    The merge information is used by git pull (which first calls git fetch) to
    lookup the default branch for merging. Without this option, git pull
    defaults to merge the first refspec fetched. Specify multiple values to
    get an octopus merge. If you wish to setup git pull so that it merges into
    <name> from another branch in the local repository, you can point
    branch.<name>.merge to the desired branch, and use the relative path
    setting . (a period) for branch.<name>.remote.

  • branch.<name>.rebase
    Cycles branch.<name>.rebase value between true, false, and the value of
    `pull.rebase`. When enabled, actions like pulling will attempt to rebase
    by default.

  • branch.<name>.pushRemote
    Cycles pushRemote between the remotes currently configured for the
    repository. `remote.pushDefault` will be offered, if set.

Arguments:                                            *anvil_branch_popup_args*
  • --recurse-submodules
    (EXPERIMENTAL) Causes the current command to recurse into submodules if
    submodule.propagateBranches is enabled.

    When used in branch creation, a new branch <branchname> will be created
    in the superproject and all of the submodules in the superproject’s
    <start-point>. In submodules, the branch will point to the submodule
    commit in the superproject’s <start-point> but the branch’s tracking
    information will be set up based on the submodule’s branches and remotes
    e.g.  git branch --recurse-submodules topic origin/main will create the
    submodule branch "topic" that points to the submodule commit in the
    superproject’s "origin/main", but tracks the submodule’s "origin/main".

Actions:                                           *anvil_branch_popup_actions*
  Actions for the branch popup are split into three categories: "Checkout",
  "Create", and "Do". They behave as described: "Checkout" actions will change
  your HEAD when checking out the selected/created branch. "Create" actions,
  by contrast, will _not_ change HEAD.

  Finally, "Do" actions provide some common housekeeping actions.

  • Checkout branch/revision                        *anvil_branch_checkout_rev*
    Changes HEAD to point to the selected branch/revision. If a local branch
    is selected, that will be checked out. Otherwise HEAD will become
    detached. Any ref under the cursor or selected will be available to
    checkout.

  • Checkout local branch                         *anvil_branch_checkout_local*
    Changes HEAD to point to the selected branch. Also lists branches on
    remotes that do not have a matching branch locally. If a remote branch is
    selected, then a local branch is created tracking the selected remote
    branch.

  • Checkout recent branch                       *anvil_branch_checkout_recent*
    Changes HEAD to point to the selected branch. Branches are listed by how
    recently they have been checked out. Only local branches are considered.

  • Checkout new branch                             *anvil_branch_checkout_new*
    Creates and switches HEAD to a new branch. Any whitespace in the branch
    name will be automatically changed to '-'.

    For example, entering "new branch name" will result in a branch called
    "new-branch-name" being created.

    After entering a name, you will be prompted to choose a base branch for
    the branch being created. The current branch will be at the top of the
    list, but any branch can be chosen.

  • Checkout new spin-off                               *anvil_branch_spin_off*
    This command creates and checks out a new branch starting at and tracking
    the current branch. That branch in turn is reset to the last commit it
    shares with its upstream. If the current branch has no upstream or no
    unpushed commits, then the new branch is created anyway and the previously
    current branch is not touched.

    This is useful to create a feature branch after work has already began on
    the old branch.

  • Checkout new worktree                      *anvil_branch_checkout_worktree*
    see: |anvil_worktree_checkout|

  • Create new branch                              *anvil_branch_create_branch*
    Functionally the same as |anvil_branch_checkout_new|, but does not update
    HEAD.

  • Create new spin-out                                 *anvil_branch_spin_out*
    Like |anvil_branch_spin_off|, but doesn't change HEAD. However, if the
    index has uncommitted changes, will behave exactly the same as spin_off.

  • Create new worktree                          *anvil_branch_create_worktree*
    see: |anvil_worktree_create_branch|

  • Configure                                          *anvil_branch_configure*
    Opens selector to choose a branch, then offering some configuration
    options. See |anvil_branch_config_popup|.

  • Rename                                                *anvil_branch_rename*
    Opens selector to choose a branch to rename. Like with a new branch, any
    whitespace will be replaced with '-'.

  • Reset                                                  *anvil_branch_reset*
    Resets the current branch to the the selected branch via a hard reset.
    Uncommitted changes will be lost in the process.

  • Delete                                                *anvil_branch_delete*
    Opens selector, allowing the user to delete either a local or remote
    branch.

  • Pull Request                                    *anvil_branch_pull_request*
    When a branch has an upstream configured, can be used to open a
    pull-request on the git services' website. By default, supports github,
    gitlab, and bitbucket, but more can be added via config.git_services.


==============================================================================
Branch Config Popup                                 *anvil_branch_config_popup*

Allows configuring some common config settings for the selected branch, as
well as some repository defaults.

Branch:
  • branch.<name>.description                 *anvil_branch_config_description*
    Branch description is automatically added in the format-patch cover letter
    or request-pull summary.

  • branch.<name>.merge                             *anvil_branch_config_merge*
    Defines, together with branch.<name>.remote, the upstream branch for the
    given branch. It tells git fetch/git pull/git rebase which branch to merge
    and can also affect git push (see push.default). When in branch <name>,
    it tells git fetch the default refspec to be marked for merging in
    FETCH_HEAD. The value is handled like the remote part of a refspec, and
    must match a ref which is fetched from the remote given by
    "branch.<name>.remote".

    The merge information is used by git pull (which at first calls git fetch)
    to lookup the default branch for merging.

    Without this option, git pull defaults to merge the first refspec fetched.
    Specify multiple values to get an octopus merge. If you wish to setup git
    pull so that it merges into <name> from another branch in the local
    repository, you can point branch.<name>.merge to the desired branch, and
    use the relative path setting . (a period) for branch.<name>.remote.

  • branch.<name>.remote                           *anvil_branch_config_remote*
    When on branch <name>, it tells git fetch and git push which remote to
    fetch from/push to. The remote to push to may be overridden with
    remote.pushDefault (for all branches). The remote to push to, for the
    current branch, may be further overridden by branch.<name>.pushRemote. If
    no remote is configured, or if you are not on any branch and there is more
    than one remote defined in the repository, it defaults to origin for
    fetching and remote.pushDefault for pushing. Additionally, . (a period) is
    the current local repository (a dot-repository), see branch.<name>.merge's
    final note below.

  • branch.<name>.rebase                           *anvil_branch_config_rebase*
    When true, rebase the branch <name> on top of the fetched branch, instead
    of merging the default branch from the default remote when "git pull" is
    run. See "pull.rebase" for doing this in a non branch-specific manner.

    NOTE: this is a possibly dangerous operation; do not use it unless you
    understand the implications (see git-rebase(1) for details).

  • branch.<name>.pushRemote                   *anvil_branch_config_pushRemote*
    When on branch <name>, it overrides branch.<name>.remote for pushing. It
    also overrides remote.pushDefault for pushing from branch <name>. When you
    pull from one place (e.g. your upstream) and push to another place (e.g.
    your own publishing repository), you would want to set remote.pushDefault
    to specify the remote to push to for all branches, and use this option to
    override it for a specific branch.

Repository defaults:
  • pull.rebase                               *anvil_branch_config_pull_rebase*
    When true, rebase branches on top of the fetched branch, instead of
    merging the default branch from the default remote when "git pull" is run.
    Use "branch.<name>.rebase" for setting this on a per-branch basis.

    NOTE: this is a possibly dangerous operation; do not use it unless you
    understand the implications (see git-rebase[1] for details).

  • remote.pushDefault                 *anvil_branch_config_remote_pushDefault*
    The remote to push to by default. Overrides branch.<name>.remote for all
    branches, and is overridden by branch.<name>.pushRemote for specific
    branches.

  • anvil.baseBranch                          *anvil_branch_config_baseBranch*
    If set, will be used by "rebase onto base" action. Otherwise defaults to
    either "main" or "master", depending on which is found.

  • anvil.askSetPushDefault            *anvil_branch_config_askSetPushDefault*
    This option controls whether the user is asked whether they want to set
    remote.pushDefault after adding a remote.

    When set to 'ask', the user is always asked.

    When 'ask-if-unset', the user is only asked if the variable isn’t set.
    (This is also the default behaviour when unset.)

    When 'never', the user isn’t asked and the variable isn’t set.

Branch creation:
  • branch.autoSetupMerge                  *anvil_branch_config_autoSetupMerge*
    Tells git branch, git switch and git checkout to set up new branches so
    that git-pull(1) will appropriately merge from the starting point branch.

    false   — No automatic setup is done.

    true    — Automatic setup is done when the starting point is a
              remote-tracking branch.

    always  — Automatic setup is done when the starting point is either a
              local branch or remote-tracking branch.

    inherit — If the starting point has a tracking configuration, it is copied
              to the new branch.

    simple  — Automatic setup is done only when the starting point is a
              remote-tracking branch and the new branch has the same name as
              the remote branch.

    This option defaults to true.

  • branch.autoSetupRebase                *anvil_branch_config_autoSetupRebase*
    When a new branch is created with git branch, git switch or git checkout
    that tracks another branch, this variable tells Git to set up pull to
    rebase instead of merge (see "branch.<name>.rebase").

    never  - Rebase is never automatically set to true.

    local  - Rebase is set to true for tracked branches of other local
             branches.

    remote - Rebase is set to true for tracked branches of remote-tracking
             branches.

    always - Rebase will be set to true for all tracking branches.

    See "branch.autoSetupMerge" for details on how to set up a branch to track
    another branch.

    This option defaults to never.

==============================================================================
Commit Popup                                               *anvil_commit_popup*

Arguments:                                            *anvil_commit_popup_args*
  • --all
    Tell the command to automatically stage files that have been modified and
    deleted, but new files you have not told Git about are not affected.

  • --allow-empty
    Usually recording a commit that has the exact same tree as its sole parent
    commit is a mistake, and the command prevents you from making such a
    commit. This option bypasses the safety, and is primarily for use by
    foreign SCM interface scripts.

  • --verbose
    Show unified diff between the HEAD commit and what would be committed at
    the bottom of the commit message template to help the user describe the
    commit by reminding what changes the commit has. Note that this diff
    output doesn’t have its lines prefixed with #. This diff will not be a
    part of the commit message. See the `commit.verbose` configuration variable
    in git-config(1).

  • --no-verify
    Skip any pre-commit or commit-msg hooks. See also githooks(5).

  • --signoff
    Add a Signed-off-by trailer by the committer at the end of the commit log
    message. The meaning of a signoff depends on the project to which you’re
    committing. For example, it may certify that the committer has the rights
    to submit the work under the project’s license or agrees to some
    contributor representation, such as a Developer Certificate of Origin.

    See http://developercertificate.org for the one used by the Linux kernel
    and Git projects.

    Consult the documentation or leadership of the project to which you’re
    contributing to understand how the signoffs are used in that project.

  • --no-gpg-sign
    This is useful to countermand both commit.gpgSign configuration variable,
    and --gpg-sign

  • --reset-author
    When used while amending or when committing after a conflicting
    cherry-pick, declare that the authorship of the resulting commit now
    belongs to the committer. This also renews the author timestamp.

  • --author=
    Override the commit author. Specify an explicit author using the standard
    A U Thor <author@example.com> format. Otherwise <author> is assumed to be
    a pattern and is used to search for an existing commit by that author
    (i.e. rev-list --all -i --author=<author>); the commit author is then
    copied from the first such commit found.

  • --gpg-sign=
    GPG-sign commits. The argument should be the keyid is optional and
    defaults to the committer identity,

  • --reuse-message=
    Take an existing commit object, and reuse the log message and the
    authorship information (including the timestamp) when creating the commit.

Actions:                                           *anvil_commit_popup_actions*
  Actions in the commit popup are in three groups: "Create", "Edit HEAD", and
  "Edit". The first should be somewhat self explanatory - you can create a new
  commit from your staged changes. "Edit HEAD" actions will modify the current
  commit HEAD is pointing at, and "Edit" will modify commits that your HEAD
  isn't pointing to.

  • Create Commit                                         *anvil_commit_create*
    Creates a new commit on the current branch from the currently staged
    changes.

  • Absorb                                                *anvil_commit_absorb*
    (Requires `https://github.com/tummychow/git-absorb`)

    `git absorb` will automatically identify which commits are safe to modify,
    and which staged changes belong to each of those commits. It will then
    write fixup! commits for each of those changes.

    The `--with-rebase` flag is passed, meaning these fixup commits will be
    automatically integrated into the corresponding ones via rebase.

  • Extend                                                *anvil_commit_extend*
    Amends the last commit without editing the commit message.

  • Reword                                                *anvil_commit_reword*
    Edit the last commit message without modifying the committed changes. Any
    staged changes are ignored.

  • Amend                                                  *anvil_commit_amend*
    Amend the last commit.

  • Fixup                                                  *anvil_commit_fixup*
    Creates a fixup commit. If a commit is selected it will be used, otherwise
    the user is prompted to pick a commit.

    `git commit --fixup=COMMIT --no-edit`

  • Squash                                                *anvil_commit_squash*
    Creates a squash commit. If a commit is selected it will be used,
    otherwise the user is prompted to pick a commit.

    `git commit --squash=COMMIT --no-edit`

  • Augment                                              *anvil_commit_augment*
    Creates a squash commit, editing the squash message. If a commit is
    selected it will be used, otherwise the user is prompted to pick a commit.

    `git commit --squash=COMMIT --edit`

  • Alter                                                  *anvil_commit_alter*
    Create a squash commit, authoring the final message now.

    During a later rebase, when this commit gets squashed into its targeted
    commit, the original message of the targeted commit is replaced with the
    message of this commit, without the user automatically being given a
    chance to edit it again.

    `git commit --fixup=amend:COMMIT --edit`

  • Revise                                                *anvil_commit_revise*
    Reword the message of an existing commit, without editing its tree.
    Later, when the commit is squashed into its targeted commit, a combined
    commit is created which uses the message of the fixup commit and the tree
    of the targeted commit.

    `git commit --fixup=reword:COMMIT --edit`

  • Instant Fixup                                  *anvil_commit_instant_fixup*
    Similar to |anvil_commit_fixup|, but instantly rebases after.

  • Instant Squash                                *anvil_commit_instant_squash*
    Similar to |anvil_commit_squash|, but instantly rebases after.

==============================================================================
Diff Popup                                                   *anvil_diff_popup*

The diff popup actions allow inspection of changes in the index (staged
changes), the working tree (unstaged changes and untracked files), any
ref range.

For these actions to become available, Anvil needs to have a diff viewer
integration enabled. Supported viewers are:
- `diffview.nvim`: https://github.com/sindrets/diffview.nvim
- `codediff.nvim`: https://github.com/esmuellert/codediff.nvim

By default, Anvil will auto-detect which viewer is available. You can
explicitly set the viewer with the `diff_viewer` option. See |anvil_setup_plugin|.

  • Diff this                                                 *anvil_diff_this*
    Show the diff for the file referred to by a hunk under the cursor.

  • Diff range                                               *anvil_diff_range*
    View a diff between a specified range of commits. Anvil presents a select
    for an `A` and a `B` ref and allows specifying the type of range.

    A two-dot range `A..B` shows all of the commits that `B` has that `A`
    doesn't have. A three-dot range `A...B` shows all of the commits that `A`
    and `B` have independently, excluding the commits shared by both refs.


  • Diff paths                                               *anvil_diff_paths*
    (Not yet implemented)

  • Diff unstaged                                         *anvil_diff_unstaged*
    Show the diff for the working tree, without untracked files.

  • Diff staged                                             *anvil_diff_staged*
    Show the diff for the index.

  • Diff worktree                                         *anvil_diff_worktree*
    Show the full diff of the working tree, including untracked files.

  • Show commit                                             *anvil_show_commit*
    Display the diff of the commits within a branch.

  • Show stash                                               *anvil_show_stash*
    Display the diff of a specific stash.


==============================================================================
Fetch Popup                                                 *anvil_fetch_popup*

Arguments:                                             *anvil_fetch_popup_args*
  • --prune
    Before fetching, remove any remote-tracking references that no longer
    exist on the remote. Tags are not subject to pruning if they are fetched
    only because of the default tag auto-following or due to a --tags option.

    However, if tags are fetched due to an explicit refspec (either on the
    command line or in the remote configuration, for example if the remote was
    cloned with the --mirror option), then they are also subject to pruning.

  • --tags
    Fetch all tags from the remote (i.e., fetch remote tags refs/tags/* into
    local tags with the same name), in addition to whatever else would
    otherwise be fetched. Using this option alone does not subject tags to
    pruning, even if --prune is used (though tags may be pruned anyway if
    they are also the destination of an explicit refspec; see --prune).

  • --force
    When git fetch is used with <src>:<dst> refspec, it may refuse to update
    the local branch. This option overrides that check. See `man git-fetch`
    for more detail.


Actions:                                            *anvil_fetch_popup_actions*
  • Fetch from pushRemote                              *anvil_fetch_pushremote*
    Fetches from the current pushRemote.

    When the pushRemote is not configured, the user will be prompted to choose
    from the existing remotes to set it.

  • Fetch from upstream                                  *anvil_fetch_upstream*
    Fetches from the upstream of the current branch.

    If the upstream is configured for the current branch and names an existing
    remote, then use that. Otherwise try to use another remote: If only a
    single remote is configured, then use that. Otherwise if a remote named
    "origin" exists, then use that.

  • Fetch from elsewhere                                *anvil_fetch_elsewhere*
    Provides a select interface for the user to choose which remote to fetch
    from.

  • Fetch from All Remotes                                    *anvil_fetch_all*
    Fetches from all configured remotes.

  • Fetch Another Branch                                   *anvil_fetch_branch*
    Fetches only a specific branch from a remote, both of which are selected
    by the user.

  • Fetch Explicit Refspec                                *anvil_fetch_refspec*
    Fetches from a remote using an explicit refspec.

  • Fetch Submodules                                   *anvil_fetch_submodules*
    Fetches all submodules.

  • Set Variables                                   *anvil_fetch_set_variables*
    Opens Branch Config popup for the current branch.
    See |anvil_branch_config_popup|.

==============================================================================
Forge Popup                                                 *anvil_forge_popup*

The forge popup integrates GitHub into Anvil, the way Forge integrates with
Magit. Requires the GitHub CLI (`gh`), authenticated via `gh auth login`, and
a GitHub remote named "origin" or "upstream". Default mapping: `N`.

Topics (pull requests, issues, and discussions) are kept in a local store under
`stdpath("data")/anvil/forge/` and rendered from disk — the network is only
used when syncing. If `kkharji/sqlite.lua` is installed, Anvil uses SQLite;
otherwise it falls back to JSON files. After a sync, "Pull requests" and
"Issues" sections appear in the status buffer (see `sections.pullreqs` and
`sections.issues` in the setup table). Discussions are synced too, but
`sections.discussions.hidden` defaults to true.

Actions:                                            *anvil_forge_popup_actions*
  • Pull topics (f f)                                         *anvil_forge_pull*
    Fetches open pull requests, issues, and discussions from GitHub into the
    local store, then refreshes the status buffer. Emits `AnvilForgePulled`.

  • Create issue (c i)                                *anvil_forge_create_issue*
    Prompts for a title, then opens an in-editor Markdown composer. Submit it
    to create the issue through `gh issue create`.

  • Create pull request (c p)                      *anvil_forge_create_pullreq*
    Prompts for a title, then opens an in-editor Markdown composer. Submit it
    to create the pull request through `gh pr create`.

  • Create discussion (c d)
    Select a discussion category, provide a title, then write the body in an
    in-editor Markdown composer.

  • Browse issues / pull requests / repo / branch (b I, b P, b r, b b)
    Opens the corresponding page on the web.               *anvil_forge_browse*

Topic buffers render stored descriptions, comments, and pull-request reviews.
Press `f` inside a topic buffer to pull fresh detail for that topic into the
local store, then re-render the buffer. Issue and pull request topic buffers
also support `c` comment with a multi-line post editor, `e` edit title, `b`
edit body in a multi-line editor, `l` add labels, `a` add assignees, `m` set
milestone, `+` add a reaction, and `s` toggle open/closed state through `gh`.
The `N` forge popup Topic group additionally offers `t c` close as completed,
`t n` close as not planned, and `t D` close as duplicate for issues, mirroring
Forge's topic state commands.
Pull request topic buffers additionally support `r` add reviewers, `R` remove
reviewers, `V` queue a pending review comment, `A` approve, `v` submit a
comment review, and `X` request changes. Successful edits refresh the local
topic detail.
Local Forge topic marks are available with `M` mark read, `u` mark unread,
`*` save or unsave, and `d` mark done; list and status buffers show `U`/`S`/`D`
markers. The `N` forge popup Topic group also offers `t N` to set or clear a
local freetext note on a topic (forge's topic-notes).
Topic list buffers default to active topics; use `A` all, `U` unread, `S`
saved, `D` done, `O` open, `C` closed, `a` author, `r` assignee, `l` label,
and `m` milestone filters. Press `s` to cycle the tablist sort column (number,
updated, state, title) and `i` to invert the sort direction; the active sort is
shown in the buffer header.
For diffview-backed reviews, the `N` forge popup Review group offers `V s`
start a PR review (opens the pull request's `base...head` diff), `V c` queue a
pending comment on the diff line under the cursor (file path and LEFT/RIGHT side
are derived from the diff window), and `V S` submit the queued review as
comment/approve/request-changes.
Pull request topic detail also renders inline review threads with file/line
context, diff hunks, resolution state, and comments. Use `i` to reply to a
numbered review thread, `x` to resolve it, and `U` to mark it unresolved.
Comment and reply editors submit on write or the commit-editor submit mapping.
Use `C` to react to a numbered topic comment and `I` to react to a numbered
review-thread comment. Suggested changes in review comments are listed in the
topic buffer; use `S` to apply a numbered suggestion to the local worktree.

  • List topics (l t)                                        *anvil_forge_list*
    Select a topic with a picker; opens the selection in a local topic buffer.
    Pressing <cr> on a status-buffer topic does the same.

  • List notifications (l n)                    *anvil_forge_notifications*
    Opens the notification list buffer. In that buffer, use `<cr>` to open a
    locally synced topic, `r` to mark read, `u` to mark unread, `s` to save or
    unsave, `d` to mark done, and `o` to explicitly open the notification URL.
    Use `g` to refresh, and `A`/`U`/`S`/`D` to filter all/unread/saved/done
    notifications. Use `t` to toggle forge's repository-grouped (nested)
    style. Read/unread/save/done state is
    stored locally; mark-read also updates GitHub when `gh` is available and
    authenticated.
    Notification polling is opt-in with:
>
      require("anvil").setup({
        forge = {
          notifications = {
            poll = true,
            interval = 300000,
          },
        },
      })
<

  • Checkout pull request (b f)                 *anvil_forge_checkout_pullreq*
    Select an open pull request and check out its branch via
    `gh pr checkout`. Emits `AnvilForgePullRequestCheckout`.

==============================================================================
Log Popup                                                     *anvil_log_popup*

Opened with `l` from most Anvil buffers or |:AnvilLog|. Runs `git log` and
opens the result in the |anvil_log_buffer|.

Arguments:                                               *anvil_log_popup_args*
  Commit Limiting:
  • `-n` `--max-count`   Limit the number of commits (default 256).
  • `-A` `--author`      Limit to a given author.
  • `-F` `--grep`        Search commit messages.
  • `-G` / `-S`          Search added/removed changes / occurrences.
  • `-L`                 Trace the evolution of a line range.
  • `-s` / `-u`          Limit to commits since / until a date.
  • `=m` `--no-merges`   Omit merge commits.
  • `=p` `--first-parent`  Follow only the first parent.
  • `-i` `--invert-grep`   Invert the message search.
  History Simplification:
  • `-D` `--simplify-by-decoration`  Simplify to the decorated history.
  • `-`                  Limit to a set of files (picker).
  • `-f` `--follow`      Follow renames for a single file.
  Commit Ordering:
  • `-r` `--reverse`     Reverse the order.
  • `-o`                 Order commits (topo/author-date/date).
  • `=R` `--reflog`      List the reflog.
  Formatting:
  • `=g` `--graph`       Show the commit graph.
  • `=c` `--color`       Colour the graph.
  • `=d` `--decorate`    Show ref names.
  • `=S` `--show-signature`  Show signatures.

Actions:                                              *anvil_log_popup_actions*
  Log:
  • `l` current          Log the current branch (or detached HEAD).
  • `h` HEAD             Log HEAD.
  • `u` related          Log commits related to the current branch.
  • `o` other            Log another branch/tag/ref (picker).
  • `L` local branches   Log all local branches.
  • `b` all branches     Log all local and remote branches.
  • `a` all references   Log everything (`--all`).
  Reflog:
  • `r` current          Reflog for the current branch.
  • `H` HEAD             Reflog for HEAD.
  • `O` other            Reflog for another branch (picker).

Inside the log buffer, pressing `l` again re-opens this popup scoped to that
buffer, re-running the log in place (magit's `magit-log-refresh`); see
|anvil_log_buffer|.

==============================================================================
Merge Popup                                                 *anvil_merge_popup*

Arguments:                                             *anvil_merge_popup_args*
  • `-f` `--ff-only`         Fast-forward only (refuse to create a merge commit).
  • `-n` `--no-ff`           Always create a merge commit.
  • `-s` `--strategy`        Merge strategy (e.g. ort, recursive, octopus).
  • `-X` `--strategy-option` Strategy-specific option (e.g. ours, theirs).
  • `-A` `--Xdiff-algorithm` Diff algorithm for the merge.
  • `-S` `--gpg-sign`        Sign the merge commit with GPG.

Actions:                                            *anvil_merge_popup_actions*
  • Merge                                                   *anvil_merge_merge*
    This command merges another branch or revision into the current branch.

  • Merge and edit message                                *anvil_merge_editmsg*
    Like `Merge` above, but opens editor to modify commit message.

  • Merge but don't commit                               *anvil_merge_nocommit*
    This command merges another branch or revision into the current branch,
    but does not actually create the merge commit, allowing the user to make
    modifications before committing themselves.

  • Absorb                                                 *anvil_merge_absorb*
    (Not yet implemented)

  • Preview Merge                                         *anvil_merge_preview*
    (Not yet implemented)

  • Squash Merge                                           *anvil_merge_squash*
    This command squashes the changes introduced by another branch or revision
    into the current branch. This only applies the changes made by the
    squashed commits. No information is preserved that would allow creating an
    actual merge commit.

    Instead of this command you should probably use a cherry-pick command.

  • Dissolve                                             *anvil_merge_dissolve*
    (Not yet implemented)

==============================================================================
Remote Popup                                               *anvil_remote_popup*

Actions:                                            *anvil_remote_popup_actions*

  • Add                                                    *anvil_remote_add*
    Add a new remote. Prompts for a name and URL. Optionally sets
    remote.pushDefault and fetches refs after adding.

  • Rename                                             *anvil_remote_rename*
    Rename an existing remote.

  • Remove                                             *anvil_remote_remove*
    Remove an existing remote.

  • Configure...                                    *anvil_remote_configure*
    Open the Remote Config popup for the selected remote.

  • Prune stale branches                       *anvil_remote_prune_branches*
    Prune stale tracking branches for the selected remote.

  • Unshallow remote                              *anvil_remote_unshallow*
    Convert a shallow remote into a full one by fetching with --unshallow.
    If the remote has exactly one fetch refspec and it does not contain a
    wildcard, you are offered the option to replace it with the standard
    refspec (+refs/heads/*:refs/remotes/<remote>/*) first.

==============================================================================
Remote Config Popup                                 *anvil_remote_config_popup*

Allows configuring some common config variables for the selected remote.

Remote:
  • remote.<name>.url                                 *anvil_remote_config_url*
    The URL of a remote repository.

  • remote.<name>.fetch                             *anvil_remote_config_fetch*
    The default set of "refspec" for git fetch.

  • remote.<name>.pushurl                         *anvil_remote_config_pushurl*
    The push URL of a remote repository

  • remote.<name>.push                               *anvil_remote_config_push*
    The default set of "refspec" for git-push

  • remote.<name>.tagOpt                           *anvil_remote_config_tagOpt*
    Setting this value to --no-tags disables automatic tag following when
    fetching from remote <name>. Setting it to --tags will fetch every tag
    from remote <name>, even if they are not reachable from remote branch
    heads.

    Passing these flags directly to git fetch can override this setting.
    See |anvil_fetch_popup_args|.

==============================================================================
Pull Popup                                                   *anvil_pull_popup*

Variables:                                         *anvil_pull_popup_variables*
  • branch.<name>.rebase
    When true, rebase the branch <name> on top of the fetched branch, instead
    of merging the default branch from the default remote when "git pull" is
    run. See "pull.rebase" for doing this in a non branch-specific manner.

Arguments:                                              *anvil_pull_popup_args*
  • --ff-only
    Only update to the new history if there is no divergent local history.
    This is the default when no method for reconciling divergent histories is
    provided (via the --rebase=* flags).

  • --rebase
    When enabled, rebase the current branch on top of the upstream branch
    after fetching. If there is a remote-tracking branch corresponding to the
    upstream branch and the upstream branch was rebased since last fetched,
    the rebase uses that information to avoid rebasing non-local changes.

    See pull.rebase, branch.<name>.rebase and branch.autoSetupRebase if you
    want to make git pull always use --rebase instead of merging.

    Note:
    This is a potentially dangerous mode of operation. It rewrites history,
    which does not bode well when you published that history already. Do not
    use this option unless you understand the consequences.

  • --autostash
    Automatically create a temporary stash entry before the operation begins,
    record it in the special ref MERGE_AUTOSTASH and apply it after the
    operation ends. This means that you can run the operation on a dirty
    worktree. However, use with care: the final stash application after a
    successful merge might result in non-trivial conflicts.

  • --tags
    Fetch all tags from the remote (i.e., fetch remote tags refs/tags/* into
    local tags with the same name), in addition to whatever else would
    otherwise be fetched.

Actions:                                             *anvil_pull_popup_actions*
  • Pull into <current> from pushRemote                 *anvil_pull_pushRemote*
    Pulls into the current branch from `branch.<name>.pushRemote`. If that is
    unset, the user will be prompted to select a remote branch, which will
    pulled from and used to set `branch.<name>.pushRemote`.

  • Pull into <current> from @{upstream}                  *anvil_pull_upstream*
    Pulls into the current branch from its upstream counterpart. If that is
    unset, the user will be prompted to select a remote branch, which will
    pulled from and set as the upstream.

  • Pull into <current> from elsewhere                   *anvil_pull_elsewhere*
    Prompts the user to select a branch (local or remote), which is then
    pulled in the current branch.

  • Set Variables                                    *anvil_pull_set_variables*
    Opens Branch Config popup for the current branch.
    See |anvil_branch_config_popup|.

==============================================================================
Push Popup                                                   *anvil_push_popup*

Arguments:                                              *anvil_push_popup_args*
  • --force-with-lease
    Usually, "git push" refuses to update a remote ref that is not an ancestor
    of the local ref used to overwrite it.

    This option overrides this restriction if the current value of the remote
    ref is the expected value. "git push" fails otherwise.

    Imagine that you have to rebase what you have already published. You will
    have to bypass the "must fast-forward" rule in order to replace the
    history you originally published with the rebased history. If somebody
    else built on top of your original history while you are rebasing, the tip
    of the branch at the remote may advance with their commit, and blindly
    pushing with --force will lose their work.

    This option allows you to say that you expect the history you are updating
    is what you rebased and want to replace. If the remote ref still points at
    the commit you specified, you can be sure that no other people did
    anything to the ref. It is like taking a "lease" on the ref without
    explicitly locking it, and the remote ref is updated only if the "lease"
    is still valid.

  • --force
    Usually, the command refuses to update a remote ref that is not an
    ancestor of the local ref used to overwrite it. Also, when
    --force-with-lease option is used, the command refuses to update a remote
    ref whose current value does not match what is expected.

    This flag disables these checks, and can cause the remote repository to
    lose commits; use it with care.

    Note that --force applies to all the refs that are pushed, hence using it
    with push.default set to matching or with multiple push destinations
    configured with remote.*.push may overwrite refs other than the current
    branch (including local refs that are strictly behind their remote
    counterpart).

  • --set-upstream
    For every branch that is up to date or successfully pushed, add upstream
    (tracking) reference.

  • --no-verify
    Skips running any pre-push git hooks. By default, all hooks are run.

  • --dry-run
    Do everything except actually send the updates.

Actions:                                             *anvil_push_popup_actions*
  • Push <current> to pushRemote                        *anvil_push_pushRemote*
    Pushes the current branch to `branch.<name>.pushRemote`. If that is unset,
    to `remote.pushDefault`. And if _that_ is unset, the user will be prompted
    to select a remote, which will be used to set `branch.<name>.pushRemote`,
    and pushed to.

  • Push <current> to @{upstream}                         *anvil_push_upstream*
    Pushes the current branch to its upstream branch. If not set, then the
    user will be prompted to select a remote, which will be set as the
    current branch's upstream and pushed to.

  • Push <current> elsewhere                             *anvil_push_elsewhere*
    Pushes the current branch to a branch selected by the user.

  • Push another branch                             *anvil_push_another_branch*
    Push a branch or commit somewhere. Both source and target are selected by
    the user.

  • Push explicit refspecs                       *anvil_push_explicit_refspecs*
    Push a refspec to a remote.

  • Push matching branches                       *anvil_push_matching_branches*
    Push all matching branches to another repository.

  • Push a tag                                                 *anvil_push_tag*
    Pushes a single tag to a remote.

  • Push all tags                                         *anvil_push_all_tags*
    Pushes all tags to selected remote. If only one remote exists, that will
    be used; otherwise the user will be prompted to select a remote.

  • Set Variables                                    *anvil_push_set_variables*
    Opens Branch Config popup for the current branch.
    See |anvil_branch_config_popup|.

==============================================================================
Rebase Popup                                               *anvil_rebase_popup*

Arguments:                                            *anvil_rebase_popup_args*
  • --keep-empty
    Keep commits that start empty before the rebase (i.e. that do not change
    anything from its parent) in the result. The default is to keep commits
    which start empty, since creating such commits requires passing the
    --allow-empty override flag to git commit, signifying that a user is very
    intentionally creating such a commit and thus wants to keep it.

    Usage of this flag will probably be rare, since you can get rid of commits
    that start empty by just firing up an interactive rebase and removing the
    lines corresponding to the commits you don’t want. This flag exists as a
    convenient shortcut, such as for cases where external tools generate many
    empty commits and you want them all removed.

  • --rebase-merges=
    By default, a rebase will simply drop merge commits from the todo list,
    and put the rebased commits into a single, linear branch. With
    --rebase-merges, the rebase will instead try to preserve the branching
    structure within the commits that are to be rebased, by recreating the
    merge commits. Any resolved merge conflicts or manual amendments in these
    merge commits will have to be resolved/re-applied manually.

    When rebasing merges, there are two modes:
      - rebase-cousins
      - no-rebase-cousins

    In no-rebase-cousins mode, commits which do not have <upstream> as direct
    ancestor will keep their original branch point, i.e. commits that would be
    excluded by git-log's --ancestry-path option will keep their original
    ancestry by default.

    In rebase-cousins mode, such commits are instead rebased onto <upstream>.

  • --update-refs
    Automatically force-update any branches that point to commits that are
    being rebased. Any branches that are checked out in a worktree are not
    updated in this way.

    If the configuration variable rebase.updateRefs is set, then this option
    can be used to override and disable this setting.

  • --committer-date-is-author-date
    Instead of using the current time as the committer date, use the author
    date of the commit being rebased as the committer date.

    This option implies --force-rebase.

  • --ignore-date
    Instead of using the author date of the original commit, use the current
    time as the author date of the rebased commit.

    This option implies --force-rebase.

  • --autosquash
    When the commit log message begins with "squash! ..." or "fixup! ..." or
    "amend! ...", and there is already a commit in the todo list that matches
    the same ..., automatically modify the todo list of rebase -i, so that the
    commit marked for squashing comes right after the commit to be modified,
    and change the action of the moved commit from pick to squash or fixup or
    fixup -C respectively.

    A commit matches the ... if the commit subject matches, or if the ...
    refers to the commit’s hash. As a fall-back, partial matches of the commit
    subject work, too. The recommended way to create fixup/amend/squash
    commits is by using the --fixup, --fixup=amend: or --fixup=reword: and
    --squash options respectively of git-commit. These are all available from
    the commit popup.

    If the --autosquash option is enabled by default using the configuration
    variable rebase.autoSquash, this option can be used to override and
    disable this setting.

  • --autostash
    Automatically create a temporary stash entry before the operation begins,
    and apply it after the operation ends. This means that you can run rebase
    on a dirty worktree. However, use with care: the final stash application
    after a successful rebase might result in non-trivial conflicts.

  • --interactive
    Make a list of the commits which are about to be rebased. Let the user
    edit that list before rebasing. This mode can also be used to split
    commits.

  • --no-verify
    This option bypasses the pre-rebase hook

  • --gpg-sign=
    GPG-sign commits.

Actions:                                           *anvil_rebase_popup_actions*
  • Rebase onto pushRemote                            *anvil_rebase_pushRemote*
    This action rebases the current branch onto its pushRemote.

    When the pushRemote is not configured, then the user can first set it
    before rebasing.

  • Rebase onto upstream                                *anvil_rebase_upstream*
    This action rebases the current branch onto its upstream branch.

    When the upstream is not configured, then the user can first set it
    before rebasing.

  • Rebase onto elsewhere                              *anvil_rebase_elsewhere*
    This command rebases the current branch onto a branch read in the picker.
    All commits that are reachable from HEAD but not from the selected branch
    TARGET will be rebased.

  • Rebase onto base                                        *anvil_rebase_base*
    Rebases the current branch onto either the user-configured base branch,
    main, or master. The base branch is read from the anvil.baseBranch git
    config, which can be set in the branch config popup.

    See |anvil_branch_config_baseBranch|

  • Rebase interactively                           *anvil_rebase_interactively*
    This action begins an interactive rebase.

  • Rebase a subset                                       *anvil_rebase_subset*
    This action starts a non-interactive rebase sequence to transfer commits
    from START to HEAD onto NEWBASE. User will be prompted to select NEWBASE,
    then a START commit.

  • Rebase to modify a commit                      *anvil_rebase_modify_commit*
    Begins an interactive rebase, allowing you to edit a single commit.

  • Rebase to reword a commit                      *anvil_rebase_reword_commit*
    Begins an interactive rebase, letting you reword an single older commit.

  • Rebase to remove a commit                      *anvil_rebase_remove_commit*
    Uses rebase to remove a single commit from the history.

  • Rebase to autosquash                              *anvil_rebase_autosquash*
    Combines `squash!` and `fixup!` commits with their target commits.

When a rebase is in progress, the following actions are available instead:
  • Continue                                            *anvil_rebase_continue*
    Continue with a rebase in progress after sorting out a conflict. May open
    the Commit editor.

  • Skip                                                    *anvil_rebase_skip*
    Skips the current commit in a rebase-todo.

  • Edit                                                    *anvil_rebase_edit*
    Edit the todo-list of the current rebase sequence.

  • Abort                                                  *anvil_rebase_abort*
    Aborts the current rebase sequence, restoring everything to how it looked
    before the rebase was attempted.

Notes:
  By default Anvil uses the --autostash argument, which causes uncommitted
  changes to be stored in a stash before the rebase begins. These changes are
  restored after the rebase completes and if possible the stash is removed.

  Even though one of the actions is dedicated to interactive rebasing, the
  popup also features the --interactive argument. This can be used to turn one
  of the other, non-interactive rebase variants, into an interactive rebase.

==============================================================================
Revert Popup                                               *anvil_revert_popup*

Arguments:                                            *anvil_revert_popup_args*
  • --mainline
    Usually you cannot revert a merge because you do not know which side of the
    merge should be considered the mainline. This option specifies the parent
    number (starting from 1) of the mainline and allows revert to reverse the
    change relative to the specified parent.

    Reverting a merge commit declares that you will never want the tree changes
    brought in by the merge. As a result, later merges will only bring in tree
    changes introduced by commits that are not ancestors of the previously
    reverted merge. This may or may not be what you want.

  • --edit
    With this option, git revert will let you edit the commit message prior to
    committing the revert. This is the default.

    Cannot be used with --no-edit

  • --no-edit
    With this option, git revert will auto-generate the commit message without
    bringing up the commit message editor.

    Cannot be used with --edit

Actions:                                           *anvil_revert_popup_actions*
  • Revert Commit(s)                                     *anvil_revert_commits*
    Reverts one or more commits. If any commits are under the cursor, or
    selected, they will be used. Otherwise a selector interface will come up.

    Will create a commit of the reverted changes.

  • Revert Changes                                       *anvil_revert_changes*
    Reverts one or more commits WITHOUT committing the worktree. If any commits
    are under the cursor, or selected, they will be used. Otherwise a selector
    interface will come up.

==============================================================================
Reset Popup                                                 *anvil_reset_popup*

All actions will default to the commit under cursor if there is one, or prompt
the user to select one if there is not.

Variables:                                        *anvil_reset_popup_variables*
  • anvil.resetThisTo                                    *anvil_reset_this_to*
    When "Commit", will pass the commit hash to `git reset` unaltered. This has
    the effect of resetting "to" the commit.

    When "Parent", will append "^" to the commit hash when calling `git reset`.
    This has the effect of resetting the commit itself.

    Only applies when a commit is selected before launching the popup. Commits
    chosen via the finder will be used unmodified, ignoring this setting.

Actions:                                            *anvil_reset_popup_actions*
  • Mixed                                                   *anvil_reset_mixed*
    Resets the index but not the working tree (i.e., the changed files are
    preserved but not marked for commit).

  • Soft                                                     *anvil_reset_soft*
    Does not touch the index file or the working tree at all (but resets the
    head to <commit>, just like all modes do). This leaves all your changed
    files "Changes to be committed", as git status would put it.

  • Hard                                                     *anvil_reset_hard*
    Resets the index and working tree. Any changes to tracked files in the
    working tree since <commit> are discarded, however a reflog entry will be
    created with their current state, so changes can be restored if needed.
    Any untracked files or directories in the way of writing any tracked files
    are simply deleted.

  • Keep                                                     *anvil_reset_keep*
    Resets index entries and updates files in the working tree that are
    different between <commit> and HEAD. If a file that is different between
    <commit> and HEAD has local changes, reset is aborted.

  • Index                                                   *anvil_reset_index*
    Reset the index to <commit>, keeping the HEAD and working tree as-is.

    For example, if the commit refers to the HEAD, then this would unstage all
    changes.

  • Worktree                                             *anvil_reset_worktree*
    Resets current worktree to specified commit.

  • Branch                                                 *anvil_reset_branch*
    see: |anvil_branch_reset|

  • File                                                     *anvil_reset_file*
    Attempts to perform a `git checkout` from the specified revision, and if
    that fails, tries `git reset` instead.

==============================================================================
Run Popup                                                     *anvil_run_popup*

Run an arbitrary git or shell command with the repository as context, magit's
`!` popup. Output is streamed to the process console, and the status buffer
refreshes when the command finishes. Commands are remembered across sessions
and offered as completion. The popup also saves Magit-style WIP refs under
`refs/wip/`. Default mapping: `!`.

Actions:                                              *anvil_run_popup_actions*
  • Git command in worktree root (!)                       *anvil_run_git_root*
    Prompts for arguments to `git` and runs them from the root of the
    worktree.

  • Git command in current directory (p)                    *anvil_run_git_cwd*
    Same, but runs from the current working directory.

  • Shell command in worktree root (s)                   *anvil_run_shell_root*
    Prompts for an arbitrary shell command and runs it from the root of the
    worktree.

  • Shell command in current directory (S)                *anvil_run_shell_cwd*
    Same, but runs from the current working directory.

  • Save WIP snapshot (w)                             *anvil_run_wip_snapshot*
    Saves dirty worktree state to `refs/wip/worktree/<branch>` without cleaning
    or stashing the worktree.

  • Save index WIP snapshot (W)                 *anvil_run_wip_snapshot_index*
    Saves staged state to `refs/wip/index/<branch>` without unstaging it.

  • List/apply WIP snapshots (l)                       *anvil_run_wip_list*
    Opens a WIP snapshot list buffer. In that buffer, press <cr> or `a` to
    apply the selected worktree or index snapshot.

Emits `AnvilUserCommandComplete` when the command finishes.

Automatic WIP snapshots are opt-in:                         *anvil_wip_auto*
>
  require("anvil").setup({
    wip = {
      enabled = true,
      before = true,
      after = false,
    },
  })
<
When enabled, Anvil writes WIP refs around mutating git operations it runs.
`before` captures state before the operation starts. `after` can additionally
capture the successful result.

==============================================================================
Patch Popup                                                 *anvil_patch_popup*

Commands for `git format-patch`, `git apply`, and `git am`.

Actions:                                            *anvil_patch_popup_actions*
  • Format patch (w, W)
  • Apply patch (a)
  • Apply mailbox (m)
  • Continue / skip / abort an in-progress `git am` (c, s, A)

==============================================================================
Notes Popup                                                 *anvil_notes_popup*

Commands for `git notes` on the selected commit or a prompted revision.

Actions:                                            *anvil_notes_popup_actions*
  • Edit, add, append, show, or remove a note (e, a, A, s, r)
  • Prune or merge notes (p, m)

==============================================================================
Submodule Popup                                         *anvil_submodule_popup*

Commands for common `git submodule` workflows.

Actions:                                        *anvil_submodule_popup_actions*
  • Add, init, update, sync, or deinit submodules (a, i, u, s, d)
  • Show submodule status or run foreach (l, f)
  • Open a submodule list buffer (L); use `u`, `s`, or `d` there to update,
    sync, or deinit the selected submodule.

==============================================================================
File Dispatch Popup                                  *anvil_file_dispatch_popup*

File-oriented commands for the current buffer path.

Actions:                                     *anvil_file_dispatch_popup_actions*
  • Stage, unstage, diff, log, or blame the current file (s, u, d, l, b)

==============================================================================
Sparse Checkout Popup                          *anvil_sparse_checkout_popup*

Commands for `git sparse-checkout`.

Actions:                                  *anvil_sparse_checkout_popup_actions*
  • Init, set, add, list, disable, or reapply sparse checkout (i, s, a, l, d, r)

==============================================================================
Subtree Popup                                           *anvil_subtree_popup*

Commands for `git subtree`.

Actions:                                          *anvil_subtree_popup_actions*
  • Add, pull, push, or split a subtree (a, p, P, s)

==============================================================================
Bundle Popup                                             *anvil_bundle_popup*

Commands for `git bundle`.

Actions:                                            *anvil_bundle_popup_actions*
  • Create, verify, list heads, or unbundle a bundle file (c, v, l, u)

==============================================================================
Shortlog Popup                                         *anvil_shortlog_popup*

Commands for `git shortlog`.

Actions:                                          *anvil_shortlog_popup_actions*
  • Run shortlog for HEAD, a prompted revision, or all refs (s, r, a)

==============================================================================
Repos Popup                                               *anvil_repos_popup*

Open a list buffer of repositories under a prompted root directory.

Actions:                                            *anvil_repos_popup_actions*
  • List repositories (l)

==============================================================================
Dispatch Popup                                         *anvil_dispatch_popup*

Global command dispatcher for opening common Anvil popups.

Actions:                                        *anvil_dispatch_popup_actions*
  • Open status, log, refs, reflog, commit, branch, stash, run, forge, patch,
    submodule, remote, or worktree commands.

==============================================================================
Mergetool Popup                                       *anvil_mergetool_popup*

Commands for resolving conflicts via `git mergetool`.

Actions:                                      *anvil_mergetool_popup_actions*
  • Run mergetool, list unmerged files, or continue after conflict resolution
    (m, g, c)

==============================================================================
Stash Popup                                                 *anvil_stash_popup*

The stash popup actions will affect the current index (staged changes) and the
working tree (unstaged changes and untracked files). When the cursor is on a
stash in the stash list, actions under the "Use" column (pop, apply and drop)
will affect the stash under the cursor.

Actions:                                            *anvil_stash_popup_actions*
  • Stash both                                               *anvil_stash_both*
    Stash both the index and the working tree.

  • Stash index                                             *anvil_stash_index*
    Stash the index only, excluding unstaged changes and untracked files.

  • Stash worktree                                       *anvil_stash_worktree*
    (Not yet implemented)

  • Stash keeping index                             *anvil_stash_keeping_index*
    Stash both the index and the working tree, but still leave behind the
    index.

  • Stash push                                               *anvil_stash_push*
    Select a changed file to push it to the stash.

  • Snapshot both                                         *anvil_snapshot_both*
    (Not yet implemented)

  • Snapshot index                                       *anvil_snapshot_index*
    (Not yet implemented)

  • Snapshot worktree                                 *anvil_snapshot_worktree*
    (Not yet implemented)

  • Snapshot to wip ref                             *anvil_snapshot_to_wip_ref*
    (Not yet implemented)

  • Pop                                                       *anvil_stash_pop*
    Apply a stash to the working tree. If there are conflicts, leave the stash
    intact. If there aren't any conflicts, remove the stash from the list.

  • Apply                                                   *anvil_stash_apply*
    Apply a stash to the working tree without removing it from the stash list.

  • Drop                                                     *anvil_stash_drop*
    Remove a stash from the stash list.

  • List                                                     *anvil_stash_list*
    Display the list of all stashes, and show a diff if a stash is selected.

  • Show                                                     *anvil_stash_show*
    (Not yet implemented)

  • Branch                                                 *anvil_stash_branch*
    (Not yet implemented)

  • Branch here                                       *anvil_stash_branch_here*
    (Not yet implemented)

  • Rename                                                 *anvil_stash_rename*
    Rename an existing stash.

  • Format patch                                     *anvil_stash_format_patch*
    (Not yet implemented)


==============================================================================
Ignore Popup                                               *anvil_ignore_popup*

Actions:                                           *anvil_ignore_popup_actions*
  • Shared at top-level                                 *anvil_ignore_toplevel*
    Appends selected filepaths to the `.gitignore` file located in the
    project's root.

  • Shared in sub-directory                               *anvil_ignore_subdir*
    Appends selected filepaths to the `.gitignore` file located in the
    selected subdirectory, creating one if it doesn't exist.

  • Private for Repo                                     *anvil_ignore_private*
    Appends selected filepaths to the user's private exclude file in the
    current repository.

  • Private Global                                        *anvil_ignore_global*
    If git config value {core.excludesFile} is set, this action will append
    selected filepaths to the global excludes file.

==============================================================================
Tag Popup                                                     *anvil_tag_popup*

Arguments:                                               *anvil_tag_popup_args*
  • --force
    Replace an existing tag with the given name (instead of failing)

  • --annotate
    Make an unsigned, annotated tag object

  • --sign
    Make a GPG-signed tag, using the default e-mail address’s key. The default
    behavior of tag GPG-signing is controlled by `tag.gpgSign` configuration
    variable if it exists, or disabled otherwise.

  • --local-user
    Make a GPG-signed tag, using the given key.

Actions:                                              *anvil_tag_popup_actions*
  • Tag                                                         *anvil_tag_tag*
    Creates a new TAG at specified REV.

  • Release                                                 *anvil_tag_release*
    Creates a release tag on HEAD. Prompts for a tag name, using the highest
    existing tag as the default so the user can increment the version.

    When --annotate is enabled, also prompts for a tag message. The proposed
    message is derived from the previous tag's message (with the old version
    string replaced by the new one), or falls back to "Project-Name X.Y.Z"
    based on the repository directory name and the new version.

  • Delete                                                   *anvil_tag_delete*
    Deletes one or more tags.

  • Prune                                                     *anvil_tag_prune*
    Offers to delete tags missing locally from REMOTE, and vice versa.

==============================================================================
Worktree Popup                                           *anvil_worktree_popup*

Actions:                                         *anvil_worktree_popup_actions*
  • Checkout worktree                                 *anvil_worktree_checkout*
    Checks out an existing branch in a new directory.

    Changes CWD.

  • Create branch and worktree                   *anvil_worktree_create_branch*
    Creates a new branch and checks it out in a new worktree, at the directory
    specified by the user.

    Changes CWD.

  • Goto                                                  *anvil_worktree_goto*
    Changes CWD to an existing worktree.

  • Move                                                  *anvil_worktree_move*
    Moves a worktree to a new directory in the filesystem.

  • Delete                                              *anvil_worktree_delete*
    Removes a worktree. If uncommitted changes or untracked files exist, user
    will be prompted to execute command forcefully. When the worktree selected
    is the CWD, then CWD will be changed to the main worktree.


==============================================================================
Status Buffer                                             *anvil_status_buffer*

The status buffer is the main Anvil interface, showing the state of the
working tree and index. It is opened via |:Anvil| or
`require('anvil').open()`.

  Actions (normal mode, default keys):

  • `s` / `S` / `<c-s>`  Stage change at point / all unstaged / everything.
  • `u` / `U`            Unstage change at point / everything staged.
  • `x`                  Discard change at point (with confirmation).
  • `-`                  Reverse the staged change at point back into the    *anvil_status_reverse*
                         working tree, keeping it staged in the index.

                         Works at hunk, file, and section granularity.
                         Cursor position determines scope:
                           - On the "Staged changes" section header → all staged files
                           - On a filename → all hunks of that file
                           - Inside an expanded hunk → that hunk only

                         Only staged changes can be reversed; attempting to
                         reverse untracked or unstaged changes shows a warning.

                         Visual-mode `V` reverses only the selected lines
                         within the hunk.

  • `<tab>`              Toggle folding for the item at point.
  • `<cr>`               Open the file at point in the editor.
  • `n` / `p` / `^`      Jump to next, previous, or parent/current section.
  • `1`..`4`, `M-1`..`M-4`
                         Show section visibility levels 1 through 4.
  • `{` / `}`            Jump to previous / next hunk header.
  • `[c` / `]c`          Scroll the file preview up / down.

  Popup shortcuts (default keys):
      `A` Cherry-pick, `b` Branch, `B` Bisect, `c` Commit, `C` Clone,
      `d` Diff, `e` Mergetool, `f` Fetch, `h` Dispatch, `i` Ignore, `l` Log,
      `L` Margin, `m` Merge, `O` Submodule, `p` Pull, `P` Push,
      `r` Rebase, `M` Remote, `T` Notes, `W` Patch, `X` Reset,
      `v` Revert, `t` Tag, `z` Stash, `Z` Worktree.

Untracked Files                                 *anvil_status_buffer_untracked*
  Anvil respects status.showUntrackedFiles. It can be set to "no" to hide
  untracked files entirely, "normal" to show files and directories (default),
  or "all" to show all files in all directories.

==============================================================================
Editor Buffer                                             *anvil_editor_buffer*

User customizations can be made via `gitcommit` ftplugin.

Commands:                                               *anvil_editor_commands*
  • Submit                                                *anvil_editor_submit*
    Default key: `<c-c><c-c>`

    Writes and closes the editor. Available in both normal and insert modes.

  • Abort                                                  *anvil_editor_abort*
    Default key: `<c-c><c-k>`

    Closes the editor buffer, discarding any changes that might have been
    written. Available in both normal and insert modes.

  • Close                                                  *anvil_editor_close*
    Default key: `q`

    Closes the editor buffer. If there are unsaved changes, user will be
    prompted to save them. Discarding the changes will abort.

  • PrevMessage                                     *anvil_editor_prev_message*
    Default key: `<m-p>`

    Replaces the current commit message with the previously used one, via
    reflog. Messages that have been discarded via reset will be included as
    well. Any changes a user makes to a message will be stored while the
    editor is open.

  • NextMessage                                     *anvil_editor_next_message*
    Default key: `<m-n>`

    Replaces the current commit message with the next used one, via
    reflog. Same semantics as |anvil_editor_prev_message| apply.

  • ResetMessage                                   *anvil_editor_reset_message*
    Default key: `<m-r>`

    If a user has changed a message that was found via reflog, reset it to how
    it appeared in reflog.

==============================================================================
Log Buffer                                                   *anvil_log_buffer*

The log buffer lists commits, optionally as a graph. It opens from the Log
popup (see |anvil_log_popup|) or |:AnvilLog|.

Mappings (normal mode; popup keys follow |anvil_setup_mappings|):
  • `<cr>`         Open the commit under the cursor in the Commit Buffer.
  • `l`            Re-open the Log popup scoped to this buffer, so adjusting
                   arguments re-runs the log in place instead of opening a new
                   split (magit's `magit-log-refresh`).
  • `+`            Load more commits (fetches the next page).
  • `o`            Open the commit under the cursor in the configured git
                   service (requires Neovim >= 0.10 for |vim.ui.open|).
  • `Y`            Yank details from the commit(s) under the cursor/selection.
  • `<tab>`        Toggle folding for the commit under the cursor.
  • `q` / `<esc>`  Close the log buffer.

The log buffer also exposes the commit-oriented popups on their usual keys,
acting on the commit under the cursor (or the visual selection): cherry-pick,
branch, commit, fetch, merge, push, rebase, remote, revert, reset, tag, pull,
bisect, and diff.

==============================================================================
Reflog Buffer                                             *anvil_reflog_buffer*

Lists reflog entries for a branch or HEAD. Opened from the Log popup Reflog
actions (see |anvil_log_popup_actions|).

Mappings (normal mode; popup keys follow |anvil_setup_mappings|):
  • `<cr>`         Open the entry's commit in the |anvil_commit_buffer|.
  • `o`            Open the entry's commit in the configured git service
                   (requires Neovim >= 0.10 for |vim.ui.open|).
  • `Y`            Yank the commit hash under the cursor.
  • `q` / `<esc>`  Close the reflog buffer.

Like the log buffer, the reflog buffer exposes the commit-oriented popups on
their usual keys, acting on the entry under the cursor (or selection):
cherry-pick, branch, commit, fetch, merge, push, rebase, remote, revert, reset,
tag, pull, bisect, and diff.

==============================================================================
Commit Buffer                                             *anvil_commit_buffer*
The commit buffer shows a single commit, including metadata, summary, and an
inline diff. It opens via |:AnvilCommit|, from log/graph views, or when
pressing `<cr>` on a commit hash elsewhere in Anvil.

Mappings (normal mode):
  • `a`          (|anvil_setup_mappings|: "OpenFileInWorktree" in commit_view)
                 On a diff hunk, try to jump to that location in the worktree
                 (i.e. current version of the file).
                 Note: it can fail if the file no longer exists or it can take
                 you to a wrong location in the file if the lines have been
                 moved around.

  • `<cr>`       On a diff hunk, open the file contents from the commit in a
                 temporary read-only tab, or the parent revision for deleted
                 lines; on a filepath line, jump to that file's diff section
                 within the buffer.

  • `o`          Open the commit in the configured git service (requires
                 Neovim >= 0.10 for |vim.ui.open|).

  • `{` / `}`      Jump to previous/next hunk or diff header.

  • `q` / `<esc>`  Close the commit buffer (keys are configurable via
               |anvil_setup_mappings| for the commit view).

  • `Y`          Opens the Yank popup, allowing copying of different details
               from the commit.

               note: To copy only a specific hunk's diff, place the
               cursor within that hunk.

  • `za` / `<tab>` Toggle folding for the current section.

  • `-`            Reverse the change at point back into the working tree.
                   (|anvil_setup_mappings|: "Reverse" in status)
                   Works at three granularities:
                   - Cursor on a hunk line → reverses that hunk only.
                   - Cursor on a diff header (e.g. "new file foo.lua") → reverses
                     all hunks for that file.
                   - Cursor on commit metadata or anywhere outside a diff →
                     reverses every change introduced by the commit.
                   In all cases the change is applied to the working tree only;
                   nothing is staged.

  • Popup shortcuts honour |anvil_setup_mappings|, default keys:
      - `A` Cherry-pick, `b` Branch, `B` Bisect, `c` Commit, `d` Diff,
        `C` Clone, `e` Mergetool, `f` Fetch, `h` Dispatch, `i` Ignore,
        `l` Log, `m` Merge, `N` Forge, `O` Submodule, `p` Pull, `P` Push,
        `r` Rebase, `T` Notes, `t` Tag, `v` Revert, `W` Patch,
        `X` Reset, `z` Stash, `Z` Worktree, `!` Run.

  • Popup transient controls:
      - `<C-x>s` Save current popup arguments as defaults.
      - `<C-x>p` Cycle previous argument histories for the popup.
      - `<C-x>r` Reset saved defaults for the popup.
      - `<C-x>l` Cycle the transient display level (1/4/7), hiding argument
        suffixes tagged above the current level (magit transient levels;
        suffixes default to level 1).

==============================================================================
Refs Buffer                                                 *anvil_refs_buffer*

Lists local branches, remote branches, and tags. Opened with `y` ("ShowRefs")
from the status buffer.

Mappings (normal mode; popup keys follow |anvil_setup_mappings|):
  • `x`            Delete the branch under the cursor or selection
                   (configurable via `refs_view.DeleteBranch`).
  • `<cr>`         Open/go to the ref under the cursor.
  • `q` / `<esc>`  Close the refs buffer.

The ref under the cursor (or the visual selection) is used as input for the
commit-oriented popups on their usual keys: cherry-pick, branch, commit, push,
rebase, revert, reset, remote, tag, pull, fetch, bisect, and diff.

==============================================================================
Rebase Todo Buffer                                   *anvil_rebase_todo_buffer*

User customizations can be made via `gitrebase` ftplugin.

The Rebase editor has some extra commands, beyond being a normal vim buffer.

The following keys, in normal mode, will act on the commit under the cursor:
• `p`    Pick current commit
• `r`    Reword current commit
• `e`    Edit current commit
• `s`    Squash current commit
• `f`    Fixup current commit
• `d`    Drop current commit
• `x`    Insert command to execute
• `b`    Insert breakpoint
• `<cr>` Open current commit in Commit Buffer

==============================================================================
Blame Buffer                                             *anvil_blame_buffer*

Opened with `:Anvil blame` from a file buffer. Shows the file with a heading
above each blame hunk: abbreviated sha, author, relative date and commit
summary, in the style of magit-blame. Uncommitted lines are grouped under an
"Uncommitted changes" heading.

The following keys are available in the blame buffer:
• `<cr>`  Open the hunk's commit in the Commit Buffer
• `b`/`B` Blame from blame: re-blame the file at the commit *before* the
          hunk's commit (rename-aware)
• `c`     Cycle the heading style: full (sha author date summary), compact
          (sha date), author (sha author), mirroring magit-blame-cycle-style
• `n`     Jump to next hunk heading
• `p`     Jump to previous hunk heading
• `Y`     Yank the hunk's full sha (uses the status `YankSelected` mapping)
• `q`/`<esc>` Close, returning to the original file and line

==============================================================================
Popup Builder                                             *anvil_popup_builder*

You can leverage Anvil's infrastructure to create your own popups and
actions. For example, you can define actions as a function which will take the
popup instance as its argument:
>lua
    local function my_action(popup)
      -- You can access the popup state (enabled flags) like so:
      local cli_args = popup:get_arguments()

      -- The numeric prefix argument (Neovim's count, the analog of magit's
      -- `C-u`) is available to every action; e.g. `3<key>` gives a prefix of 3.
      if popup:has_prefix() then
        local n = popup:get_prefix()
        vim.notify("Invoked with prefix " .. n)
      end

      -- You can use Anvil's git abstraction for many common operations
      -- local git = require("anvil.lib.git")

      -- The input library provides some helpful interfaces for getting user
      -- input
      local input = require("anvil.lib.input")
      local user_input = input.get_user_input("User-specified free text for the action")

      vim.notify(
        "Hello from my custom action!\n"
        .. "CLI args: `" .. table.concat(cli_args, " ") .. "`\n"
        .. "User input: `" .. user_input .. "`")
    end

    function create_custom_popup()
      local popup = require("anvil.lib.popup")
      local p = popup
        .builder()
        :name("AnvilMyCustomPopup")
        -- A switch is a boolean CLI flag, like `--no-verify`
        :switch("s", "my-switch", "My switch")
        -- An "_if" variant exists for builder methods, that takes a boolean
        -- as its first argument.
        :switch_if(true, "S", "conditional-switch", "This switch is conditional")
        -- Options are CLI flags that have a value, like `--strategy=octopus`
        :option("o", "my-option", "default_value", "My option", { key_prefix = "-" })
        :new_action_group("My actions")
        :action("a", "Some action", my_action)
        -- Data can be stored on the popup instance via the `env`, accessible
        -- to the action via `popup.state.env.*`
        :env({ some_data = { "like this" } })
        :build()

      p:show()

      return p
    end

  require("anvil")
<

Look at the builder APIs in `lua/anvil/lib/popup/builder.lua`, the built-in
popups/actions in `lua/anvil/popups/*`, and the git APIs in
`lua/anvil/lib/git` for more information (and inspiration!).

To access your custom popup via a keymapping, you can include a mapping when
calling the setup function:
>lua
    require("anvil").setup({
      mappings = {
        status = {
          ["<leader>A"] = create_custom_popup,
        },
      },
    })
<

==============================================================================
Customizing Popups                                        *anvil_custom_popups*

You can customize existing popups via the Anvil config.

Below is an example of adding a custom switch, but you can use any function
from the builder API.
>lua
    require("anvil").setup({
      builders = {
        ---@param builder PopupBuilder
        AnvilPushPopup = function(builder)
          builder:switch('m', 'merge_request.create', 'Create merge request', { cli_prefix = '-o ', persisted = false })
        end,
      },
    })

Keep in mind that builder hooks are executed at the end of the popup
builder, so any switches or options added will be placed at the end.

------------------------------------------------------------------------------
vim:tw=78:ts=8:ft=help:norl:
